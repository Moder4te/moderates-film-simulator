# FilmSim v2 설계 문서

> v2questions.md의 답변과 UXP 실측 결과를 반영한 확정 설계.
> 원본 아이디어 항목(00~06)은 부록 A에 보존하고, 본문에서 번호로 참조한다.

---

## 1. 확정 사항

| 항목 | 결정 | 근거 |
|---|---|---|
| 엔진 방식 | **3D LUT 기반** | A1 |
| LUT 적용 방법 | **`imaging.putPixels` 직접 적용** | Color Lookup 자동화 불가 확정 (부록 B) |
| 비트 심도 | **16bit/채널** | 32bpc에서 Selective Color 등 비활성 |
| 작업 색공간 | **ProPhoto RGB (γ1.8)**, 출력 sRGB | B2 |
| raw 진입점 | **Camera Raw → Photoshop** | B4 |
| 표준 입력 상태 | 카메라 프로파일 중립 + 톤 슬라이더 0 + WB만 조정 | B1 |
| 필름 데이터 출처 | **제조사 TDS만**. 컬러체커 촬영본은 후일 확장 | C1/C2 |
| 정확도 기준 | 감성 근사. "정답 스캔" 검증 없음 | C3 |
| 스캐너 | Noritsu / Frontier 2종 | D3 |
| 필름 포맷 | **사용자 선택** (35mm / 120 / 대형) | 그레인 물리의 전제 |
| 기준 해상도 | **2단계 — 6000×4000 / 9000×6000**, DPI 없으면 300 가정 | E1 |
| 노광 보정 | **지원** (푸시/풀) | 신규 결정 |
| .cube 익스포트 | ProPhoto 기준 1종. **영상용 Rec.709는 추후** | E2 + 사진 집중 |
| 프리뷰 | 저해상도 프록시 + 풀해상도 2단계 (필수) | F3, Worker 부재 |
| 배포 | 오픈소스 / 비상업 | G1/G2 |
| 하위호환 | v1 프리셋 호환 불필요 | H3 |
| 릴리스 | 단계별 (v1.4 → v1.5 → v1.6 → v2.0) | H2 |

### 실측으로 확정된 런타임 제약

| 기능 | 상태 | 설계 영향 |
|---|---|---|
| WebGL / WebGL2 | ❌ 전부 부재 | 06번을 네이티브 LUT 레이어로 재정의 |
| `OffscreenCanvas` | ❌ undefined | — |
| `canvas.getImageData` | ❌ 없음 | 캔버스 픽셀 리드백 불가 |
| `Worker` | ❌ undefined | **JS 멀티스레드 불가**. 모든 연산 단일 스레드 |
| WebAssembly | ✅ 실행 확인 | 다만 **불필요** — 순수 JS로 목표 달성 (6장) |
| SharedArrayBuffer / Atomics | ✅ | Worker가 없어 실익은 제한적 |
| `doc.width/height/resolution` | ✅ 즉시 읽힘 | 01번 그레인 스케일링 성립 |
| `doc.bitsPerChannel` / `doc.mode` | ✅ | 입력 검증에 사용 |
| `imaging.getPixels` / `putPixels` | ✅ 검증됨 | **LUT 적용의 유일한 경로** |
| Color Lookup 레이어 **생성** | ✅ 16bit ProPhoto에서 정상 | 레이어는 만들어짐 |
| Color Lookup에 **.cube 주입** | ❌ **불가 확정** | batchPlay가 LUT 데이터를 못 나름 |
| batchPlay descriptor의 바이너리 | ❌ `ByteArray is not supported` | ArrayBuffer·Uint8Array 모두 거부 |
| `Adobe Camera Raw Filter` 구동 | ✅ 가능 | v2.0 연구 과제 (7장) |

---

## 1.5 도구 이분화 — v1과 v2의 역할 재정의

**2026-07-25 결정.** 애초 계획은 v1 그레이딩을 걷어내고 v2가 전부 흡수하는
것이었으나, 실기 테스트를 거치며 **두 도구로 나누는 편이 낫다**는 판단으로 바꿨다.

| | v2 (필름 엔진) | v1 (레거시 그레이딩) |
|---|---|---|
| 역할 | **XML(프로파일) 생성** | **JPEG 후처리** |
| 입력 | 없음 (필름 데이터에서 생성) | XML로 현상된 JPEG |
| 출력 | ACR/LR 프로파일 | 픽셀 |
| 담당 | 색 — 유제 + 스캐너 | 할레이션 · 그레인 · 가벼운 그레이딩 |
| 적용 시점 | RAW 현상 단계 | 현상 후 |

### 왜 나누는가

**색은 RAW 현상 단계에서 적용하는 것이 옳다.** 16bit 선형 데이터에 LUT을 먹이는
쪽이 이미 렌더된 픽셀을 다시 주무르는 것보다 계조 손실이 적다. 그리고 Lightroom
사용자는 Photoshop을 거치지 않는다 — A3에서 확정한 프로파일 배포 방식과도 맞는다.

**할레이션과 그레인은 반대다.** 둘 다 공간 연산이라 LUT에 담기지 않고(4.1),
프로파일로도 표현할 수 없다. 픽셀을 직접 만져야 하므로 Photoshop이 제자리다.

즉 **LUT에 담기는 것은 프로파일로, 담기지 않는 것은 픽셀 도구로** 갈라진다.
경계가 기술적으로 명확해서 애매한 중간이 없다.

### v1 그레이딩의 위상 변화

당초 "제거 대상"이었으나 **JPEG 보정 도구로 존속**한다. 프로파일로 현상한 뒤
남는 미세 조정(색역별 CMY, 토우/숄더)을 담당한다. 이미 Photoshop 조정 레이어로
비파괴 동작하므로 그 용도에 맞다.

### 어떤 XML인가 — 해결

ACR 프로파일에는 두 갈래가 있다.

| | 형식 | 정밀도 | 쓸 수 있는가 |
|---|---|---|---|
| 프리셋 | `crs:ToneCurvePV2012`, HSL, ColorGrade 등 평문 | 중 | 가능 — 전부 평문 XMP |
| Look 프로파일 | `crs:RGBTable` + `crs:Table_<ID>` 3D LUT | 높음 | **가능** — 아래 |

Look 프로파일을 골랐다. 3D LUT을 그대로 담을 수 있는 유일한 형식이고, 프리셋
경로는 우리 LUT을 곡선·HSL로 **재표현**해야 해서 정보가 손실된다.

한동안 이쪽을 "막힘"으로 적어뒀는데 **오판이었다.** `crs:Table_<ID>` 인코딩을
직접 해독하려 했고(부록 B), 229,809자 페이로드를 ASCII85 계열로 풀어봤지만
압축 시그니처도 없고 길이도 홀수라 실패했다. 그래서 비공개 포맷이라 결론지었다.

실제로는 **Adobe가 변환 도구를 Photoshop 안에 넣어놨다.** Camera Raw의 Presets
패널에서 Alt(Option)를 누른 채 New Preset을 누르면 "New Profile" 대화상자가 뜨고,
Color Look-Up Table만 체크한 뒤 .cube를 지정하면 크리에이티브 프로파일 XMP가
만들어진다. Lightroom / Lightroom Classic / Camera Raw의 Profile Browser에 그대로
뜨고 강도 슬라이더도 붙는다. 별도로 Adobe **Profiles SDK**(LrC 7.3 / ACR 10.3
시점 공개)도 있어 프로그램 생성 경로가 열려 있다.

즉 **우리는 .cube까지만 만들면 된다.** 인코딩 해독은 불필요한 우회였다.

```
film.js → lut.js.toCube() → .cube → [ACR New Profile 또는 Profiles SDK] → .xmp
```

교훈 — 남의 파일 포맷을 뜯기 전에 **그 회사가 만드는 도구를 먼저 찾아라.**
NLP 같은 서드파티가 `crs:RGBTable`을 쓸 수 있는 것도 비밀을 알아서가 아니라
이 공개 경로를 쓰기 때문일 가능성이 높다.

**남은 검증** — ACR의 New Profile이 .cube를 **어느 색공간 전제로** 읽는지는 아직
실측하지 않았다. ACR 내부 작업 공간은 ProPhoto 원색 + sRGB 톤 응답으로 알려져
있으나 확인 전이다. 그래서 `cubeexport.js`가 두 공간을 모두 내보낸다(§7).

## 2. v1에서 무엇이 바뀌는가

### 없어지는 것

| v1 | 처리 |
|---|---|
| `grading.js`의 Selective Color batchPlay | **삭제.** LUT 한 장으로 대체 |
| `grading.js`의 Channel Mixer 크로스토크 | **삭제.** LUT에 흡수 |
| `grading.js`의 Curves 레이어 | **삭제.** LUT에 흡수 |
| `simulate.js` 전체 | **삭제.** LUT 직접 조회로 대체 |
| `params.grading.selectiveColor` (9색역 × CMYK) | 스키마에서 제거 |

`simulate.js`가 사라지는 것이 v2의 조용한 최대 수확이다. 현재 이 파일 상단에는
"Selective Color는 Photoshop 내부 알고리즘이 비공개라 색역 판정 기반 근사"라는
주석이 달려 있고, 그래서 **패널 미리보기와 실제 적용 결과가 원리적으로 일치하지
않았다**. v2에서는 미리보기와 최종 적용이 같은 LUT을 조회하므로 이 불일치가
구조적으로 소멸한다.

### 남는 것

| v1 | v2에서의 위치 |
|---|---|
| `halation.js` | **그대로 유지.** 공간 연산이라 LUT에 담기지 않음 |
| `grain.js`의 존별 Blend If 구조 | 유지. 단 크기 계산만 물리 기반으로 교체 |
| `pipeline.js`의 레이어 정리/그룹화 | 유지 |
| `ps.js` batchPlay 래퍼 | 유지 + Color Lookup 명령 추가 |
| `presets.js` | 스키마 변경에 맞춰 재작성 |

---

## 3. 새 파이프라인

```
Camera Raw (중립 현상, ProPhoto 16bit)
   ↓
[입력 검증]  bitsPerChannel / mode / 색공간 확인, 필요 시 승격·경고
   ↓
┌─ LUT 엔진 ──────────────────────────────────┐
│  필름 TDS 데이터                              │
│    → D-logE 특성곡선 (R/G/B 층별)             │
│    → 노광 보정 시프트 (푸시/풀)                │
│    → 분광 감도 기반 크로스토크 3×3            │
│    → 스캐너 스테이지 (Noritsu / Frontier)     │
│    → 33³ 또는 65³ 격자 굽기                   │
└──────────────┬───────────────────────────────┘
               ├──→ .cube 파일 익스포트 (사용자 배포용)
               ↓
   stampVisible → getPixels → 사면체 보간 LUT 적용 → putPixels
   → "FilmSim · Color" 픽셀 레이어                    ← 색은 여기서 끝
               ↓
        할레이션 (v1 방식 유지)
               ↓
        그레인 (물리 기반 스케일링)
               ↓
        FilmSim 그룹으로 묶기
```

색 적용이 조정 레이어가 아니라 **픽셀 레이어**인 것이 v1 대비 가장 큰 변화다.
Color Lookup 조정 레이어에 우리 LUT을 실을 수 없다는 것이 실측으로 확정됐기
때문이다(부록 B). 대신 v1의 할레이션·그레인이 이미 픽셀 레이어를 쌓는 구조이므로
파이프라인 성격 자체는 일관된다 — `stampVisible`로 하단을 합쳐 읽고, 결과를
새 레이어로 올린 뒤 그룹으로 묶는 흐름이 그대로 유지된다.

v1의 순서 원칙(색 → 할레이션 → 그레인)은 그대로 유지한다. 그레인을 색보다 먼저
적용하면 이후 보정에 노이즈가 증폭된다는 이유가 v2에서도 동일하게 유효하다.

---

## 4. 엔진 상세

### 4.1 LUT 엔진 (원본 03 + 04 + 05 + 06 통합)

네 항목이 별개 기능이 아니라 **하나의 엔진의 서로 다른 단면**이라는 것이 06번
실측의 결론이다.

- **04 (센시토메트리)** = LUT의 입력 데이터
- **03 (사면체 보간)** = LUT를 굽는 방법
- **05 (.cube)** = LUT의 직렬화 형식
- **06 (GPU 가속)** = LUT를 PS에 적용하는 방법

#### 사면체 보간의 실제 적용 지점

putPixels 방식으로 가면서 **03번이 원래 의도대로 최종 이미지에 적용된다.**
Color Lookup 방식이었다면 적용 시점의 보간은 Photoshop이 담당해 우리가 제어할 수
없었을 것이다. 직접 적용하므로 세 군데 모두에서 우리 보간이 쓰인다.

1. **LUT을 굽는 과정** — TDS 곡선은 이산 샘플이므로 격자점 색을 계산할 때 곡선
   사이를 보간해야 한다.
2. **최종 이미지 적용** — 격자점 사이 색을 조회할 때. 감산 혼합 구간의 밴딩 방지라는
   원본 03번의 목적이 여기서 직접 달성된다.
3. **프록시 프리뷰** — 패널 미리보기에서 LUT을 조회할 때.

Color Lookup 경로가 막힌 것에 대한 유일한 보상이다.

#### 격자 크기

| 용도 | 크기 | 파일 크기 |
|---|---|---|
| 프록시 프리뷰 | 33³ | ~1.2MB |
| 최종 적용 / 익스포트 | 65³ | ~9MB |

#### LUT 생성 비용 — 실측

| 크기 | 격자점 | 생성 시간 |
|---|---|---|
| 17³ | 4,913 | 1ms |
| 33³ | 35,937 | **1ms** |
| 65³ | 274,625 | **2ms** |

사실상 공짜다. **캐시 설계가 필요 없다.** 파라미터가 바뀔 때마다 새로 구우면 되고,
노광 보정 13단계를 전부 즉석 생성해도 부담이 없다. 4.4의 "요청 시 생성" 방침이
실측으로 확정됐다.

### 4.2 센시토메트리 → LUT 변환

TDS에서 얻는 것과 못 얻는 것을 분명히 구분한다. 이 경계가 파이프라인 분리의
근거다.

**TDS에 있는 것**
- D-logE 특성곡선 (R/G/B 층별, Status M 농도) → 톤·색 응답의 뼈대
- 분광 감도 곡선 → 크로스토크 3×3 유도
- 분광 염료 농도 → 색상 시프트 방향
- **Print Grain Index (PGI)** → 그레인 엔진의 근거 (아래 참조)

#### ⚠️ RMS 그래뉼래러티는 더 이상 발행되지 않는다

착수 전에는 TDS의 RMS 그래뉼래러티로 물리적 입자 크기를 유도할 계획이었다.
실제 TDS를 받아보니 최신 Kodak 자료에는 그 값이 없다. 본문에 명시돼 있다.

> Print Grain Index … **It replaces rms granularity and has a different scale
> which cannot be compared to rms granularity.** (E-4050 / E-7022, p3)

PGI는 물리적 입자 크기가 아니라 **확산 인화물의 지각적 거칠기 척도**다.
4단위 차이가 관찰자 90%에게 인지 가능한 차이(JND)이고, 25가 가시 임계값이다.

실측한 값 (35mm, 4×6 인화, 4.4배 확대):

| 필름 | PGI | 비고 |
|---|---|---|
| Portra 400 | **37** | T-GRAIN 유제 |
| Gold 200 | **44** | ISO는 절반인데 더 거칠다 |

두 가지가 드러났다.

1. **PGI는 배율의 함수다.** Portra 400은 35mm를 4.4배 확대할 때 37인데, 120을
   4.4배 확대해도 **똑같이 37**이다. 즉 포맷이 아니라 확대율이 거칠기를 정한다 —
   4.5의 "입자 크기 ∝ 배율" 물리와 정확히 부합한다. 접근은 유효하다.
2. **µm 단위 입자 크기로는 변환할 수 없다.** TDS가 명시적으로 비교 불가라고 못
   박았으므로, 4.5의 `effectiveGrainMicrons`를 TDS에서 유도할 방법이 없다.

**대응** — 필름 정의에 `printGrainIndex`(기준 배율에서의 값)를 두고, 이를 **필름
간 상대 거칠기**로 쓴다. 절대 크기는 기준 필름 하나를 눈으로 맞춰 보정(calibration)
하고 나머지는 PGI 비로 스케일한다. 물리적 유도가 아니라 **한 점 보정 + 상대
스케일**이 되는 셈이고, C3(감성 근사)의 범위 안이다. v1.5 착수 시 확정한다.

**TDS에 없는 것**
- 오렌지 마스크 제거 / 네거 반전 알고리즘
- 스캐너의 색 렌더링
- 인화지 응답

사람들이 "Portra 400 색감"이라 부르는 것의 절반 이상이 없는 쪽에 있다. 따라서
**02번 스캐너 레이어는 옵션이 아니라 룩의 주력**이다.

- **에멀전 스테이지 (TDS 기반)** = 물리적 뼈대. 데이터가 말하는 대로.
- **스캐너 스테이지 (경험적 튜닝)** = 감성 손잡이. 여기서 룩이 완성된다.

C3에서 "감성 근사로 OK"라고 정했으므로 이 구조가 정직하고 일관된다.

#### 스캐너 화이트포인트 정규화 (구현 중 추가된 단계)

실효대비(곡선 기울기 × printGamma)가 1을 넘는 층은 v=1에서 출력이 1.0을 초과한다.
Portra의 청감층이 실효대비 1.085라 `0.18 × 5.56^1.085 = 1.16`이 된다. 그대로 두면
**층마다 다른 지점에서 잘려 하이라이트의 색이 무너진다.**

실제 스캐너는 필름의 농도 범위를 출력 범위에 맞춰 노출을 잡는다. 같은 일을 한다 —
세 층 공통의 스칼라 이득이므로 **채널 비율(=색상)은 보존되고 전체 밝기만 내려간다.**
그 결과 기준 그레이가 0.2~0.3스톱 정도 어두워지는데, 대비가 센 필름에서 실제로
나타나는 거동이라 정상으로 본다.

**이득은 노광 보정 0을 기준으로 고정한다.** 현재 노광에서 매번 다시 계산하면
노광을 올릴수록 하이라이트가 밝아져 이득이 더 세게 걸리고, 결과적으로 노광 보정이
스스로를 상쇄한다 — 구현 중 실제로 +1스톱이 0스톱보다 어두워지는 것을 테스트가
잡았다. 스캐너도 화이트포인트를 필름 기준으로 한 번 정하지 매 컷 다시 잡지 않는다.

#### 이 단계가 남기는 부작용 — 스캐너 스테이지가 필요한 이유

이득이 **가장 가파른 층(보통 청감층)에 맞춰지므로**, 채널 대비 편차가 큰 필름일수록
나머지 층이 더 눌린다. 필름 7종 실측:

| 필름 | 실효대비 R ~ B | 기준 그레이 | 흰색 입력의 출력 |
|---|---|---|---|
| Gold 200 | 0.967 ~ 1.102 | −0.13스톱 | (0.944, 0.969, 1.000) |
| Portra 400 | 0.967 ~ 1.144 | −0.37스톱 | (0.857, 0.862, 1.000) |
| Ektar 100 | 0.978 ~ 1.154 | −0.44스톱 | (0.823, 0.845, 1.000) |
| **Agfa Ultra 50** | 0.932 ~ 1.142 | **−0.67스톱** | **(0.765, 0.822, 1.000)** |

기준 그레이는 정확히 중성으로 유지되지만(편차 1e-4 수준), 거기서 멀어질수록 채널이
갈라진다. 앵커에서 2.4스톱 위인 흰색에서는 Ultra 50 기준 R과 B가 0.5스톱 벌어져
**흰색이 뚜렷한 청색 편향으로 렌더된다.**

이것은 결함이 아니라 **유제 단계만 있고 스캐너 단계가 없기 때문**이다. 실제
스캐너·인화는 채널별 보정으로 이 발산을 중화한다. 4.2에서 "룩의 절반 이상은
스캐너·반전 단계에 있고 TDS에는 그 정보가 없다"고 적었는데, 그 공백이 여기서
수치로 드러난다. **02번 스캐너 스테이지(v1.5)가 메워야 할 자리가 정확히 이것이다.**

그때까지는 필름 간 **상대 비교**로 읽어야 한다. 절대적인 색 재현을 기대할 단계가
아니다(C3의 "감성 근사" 범위).

필름 정의에 `whitePoint: "clip"`을 주면 이 단계를 끌 수 있다.

#### 필름 종류 — 네거티브 / 리버설 / 흑백

`film.type`이 4단계(반전/인화)의 **부호와 정규화 기준**을 정한다. 종류를 적지
않으면 엔진이 거부한다 — 빠뜨렸을 때 조용히 네거티브로 처리하면 리버설 필름이
통째로 뒤집힌 채 나온다.

| | `color-negative` | `color-reversal` | `bw-negative` |
|---|---|---|---|
| 노광↑ | 농도↑ | **농도↓** | 농도↑ |
| 4단계 | `P = 0.18·10^(+pg·(D−D₀))` | `P = 10^(−pg·(D−D_white))` | 네거티브와 동일 |
| 정규화 기준 | 기준 그레이 → 0.18 | **가장 밝은 입력 → 1.0** | 기준 그레이 → 0.18 |
| `printGamma` | ≈ 1/γ_G (대비 1.0) | **1.0** (슬라이드 대비가 곧 룩) | ≈ 1/γ |
| 층 | 3 | 3 | **1** |
| 크로스토크 | 사용 | 사용 | 무의미 |
| 화이트포인트 정규화 | 적용 | **건너뜀** (응답 안에서 이미 처리) | 적용 |

**리버설의 정규화 기준이 핵심이다.** 슬라이드는 그 자체가 볼 수 있는 포지티브이고
눈에 보이는 밝기가 곧 투과율 10^(−D)다. 그래서 인화 단계가 없고 `printGamma`가
1.0이며, 필름의 가파른 대비(Velvia는 |γ|≈2)가 그대로 룩이 된다. 네거티브처럼
대비를 1.0으로 되돌리면 슬라이드를 쓰는 의미가 사라진다.

기준을 "곡선 전체의 최소 농도(Dmin)"로 잡으면 안 된다. 구현 중 실제로 그렇게
했다가 **최대 출력이 0.0099까지 떨어져 화면이 새까맣게 나왔다.** 입력이 도달하지
못하는 노광 구간의 농도까지 기준이 되기 때문이다. 스캐너가 하는 일은 "필름이 낼
수 있는 최저 농도"가 아니라 **"장면의 가장 밝은 곳"을 흰색에 놓는 것**이다.
그 기준도 노광 보정 0에서 고정한다 — 아니면 노광 보정이 스스로를 상쇄한다.
결과적으로 +노광은 흰색을 넘어 잘리는데, 슬라이드가 실제로 그렇게 날아간다.

**흑백은 구조가 다르다.** 층이 하나라 크로스토크와 층별 감마가 의미를 잃는다.
RGB를 **선형 공간에서** 하나의 노광량으로 합친 뒤 단일 곡선을 태우고 무채색으로
내보낸다. 그래서 컬러 경로의 "축마다 한 번만 계산" 최적화를 쓸 수 없다 — 채널을
섞은 뒤에 곡선을 타므로 격자점마다 계산해야 한다. 곡선이 배열 조회라 65³도 32ms다.

`spectralWeights`가 흑백 필름의 성격을 만든다. TDS의 분광 감도 곡선에서 유도하는
것이 정석이고, 없으면 범색성 근사(0.30/0.59/0.11)를 쓴다. **적색 비중을 키우면
하늘이 어두워지고 피부가 밝아진다** — 황색·적색 필터 시뮬레이션도 결국 이 가중치를
바꾸는 일이라, 필터를 나중에 얹을 자리가 이미 열려 있다.

### 4.3 필름 데이터 포맷

필름 한 종 = 정의 하나(`src/films.js`). **수치는 TDS PDF의 벡터 좌표에서 직접
추출한다** — 그래프를 눈으로 디지타이징하지 않는다.

#### 추출 방법 (2026-07-25 확립)

Kodak TDS의 특성곡선은 **래스터 이미지가 아니라 벡터 경로**다(두 PDF 모두 이미지
XObject 0개). 따라서 곡선 좌표를 그대로 읽어 축 기하로 변환하면 눈대중 없이 정확한
값이 나온다. 절차와 함정은 부록 C에 기록했다.

정확도 확인 — 벡터 추출값과 **독립 시각 판독 4건**(필름당 2명, 이미지만 보고 판독)을
대조한 결과 농도 **0.01 이내**로 일치했다. 직선부 기울기는 소수 셋째 자리까지 같았다.

```jsonc
{
  "id": "kodak-portra-400",
  "displayName": "Portra 400",
  "manufacturer": "Kodak",
  "type": "color-negative",
  "iso": 400,
  "balance": "daylight",          // "daylight" | "tungsten"
  "source": {
    "document": "Kodak Alaris E-4050 (Rev. 2-16), p4 Characteristic Curves",
    "retrieved": "2026-07-25",
    "method": "PDF 벡터 경로 좌표 추출",
    "logHRefAbsolute": -1.44,     // TDS의 Log H Ref. 이 지점이 H=0이 된다
    "note": "주광 노광, Status M 농도. 원본 범위 logE -3.44 ~ +0.56."
  },

  // D-logE 특성곡선. x = 상대 로그 노광 H (H=0 이 Log H Ref), y = 농도.
  // 0.2 간격 재샘플. 층별로 샘플 수가 달라도 된다.
  "characteristicCurves": {
    "r": [[-2.0, 0.219], [-1.8, 0.225], /* … 21점 … */ [2.0, 2.015]],
    "g": [[-2.0, 0.646], [-1.8, 0.650], /* … */         [2.0, 2.460]],
    "b": [[-2.0, 0.867], [-1.8, 0.873], /* … */         [2.0, 3.050]]
  },

  // 녹감층 실효대비가 1.0이 되도록 잡는다 (1/γ_G).
  "printGamma": 1.806,

  // TDS에 층간 크로스토크 데이터가 없다. 분광 감도에서 유도하기 전까지 항등.
  "crosstalk": [[100, 0, 0], [0, 100, 0], [0, 0, 100]],

  // 그레인 (01번 입력). RMS가 아니라 PGI다 — 4.2의 경고 참조.
  "grain": { "printGrainIndex": 37, "atMagnification": 4.4, "format": "35mm" },
  // 노광 보정 지원 범위 (스톱)
  "exposureRange": [-2.0, 2.0]
}
```

#### 전사 결과 (2026-07-25)

| | Portra 400 | Gold 200 |
|---|---|---|
| 문서 | E-4050 (Rev. 2-16) | E-7022 |
| Log H Ref | −1.44 | −1.14 |
| 곡선 범위 (절대 logE) | −3.44 ~ +0.56 | −3.00 ~ +0.86 |
| 샘플 | 21점 (H −2.0~2.0) | 20점 (H −1.8~2.0) |
| γ R / G / B | 0.536 / 0.554 / 0.634 | 0.539 / 0.557 / 0.613 |
| printGamma | 1.806 | 1.796 |
| 실효대비 R / G / B | 0.967 / 1.001 / 1.144 | 0.967 / 1.000 / 1.102 |
| PGI (4.4배) | 37 | 44 |

**두 필름의 감마가 거의 같다.** 착수 전에는 Gold가 훨씬 대비가 셀 것으로 보고
자리표시자를 그렇게 넣었는데, 실제로는 녹감층 기울기가 0.554 대 0.557로 사실상
동일하다. 차이는 **청감층 대비**에 있다 — Portra 1.144, Gold 1.102. 즉 Portra가
오히려 하이라이트가 더 차갑고 암부가 더 따뜻하게 기운다.

이것은 **유제만의 특성**이고, 사람들이 아는 "Portra 색감"과 다를 수 있다. 4.2에서
정리한 대로 그 룩의 절반 이상은 스캐너·반전 단계에 있으며, TDS에는 그 정보가 없다.
02번 스캐너 스테이지(v1.5)가 그 간극을 메우는 자리다.

#### 단조성 보정

Gold 200의 청감층은 발끝(H −1.8 ~ −1.5)에서 농도가 0.992 → 0.965로 **내려갔다가**
다시 오른다. 선 두께보다 큰 하강이라 조판 오차가 아니라 원도면에 그렇게 그려져
있고, 독립 판독 2건이 같은 것을 확인했다.

노광이 늘며 농도가 주는 일은 실제 필름에서 일어나지 않으므로 엔진이 막는다
(`curve.js`의 PAVA 단조 보정). **데이터 파일은 TDS 그대로 두고 엔진에서 보정한다** —
측정값은 정직하게 남기고, 물리적으로 불가능한 입력만 방어하는 쪽이 맞다.

딥 자체보다 **외삽으로 번지는 영향이 컸다.** 곡선 시작점의 기울기가 음수(−0.135)라
데이터 범위 아래로 선형 외삽하면 그 음의 기울기가 검정까지 이어져, 심부 암부에서
밝아질수록 청색이 어두워졌다(측정: 8비트 4계단). 보정 후 하강폭 0.000.

### 4.4 노광 보정 (푸시/풀)

D-logE 곡선을 쓰면 노광 보정이 자연스럽게 따라온다. 단순 밝기 조정과 결정적으로
다른 점은, **채널별 곡선 기울기가 달라서 노광을 옮기면 색이 함께 이동한다**는
것이다. 푸시 현상한 필름이 콘트라스트가 오르면서 색까지 틀어지는 현상이 여기서
공짜로 재현된다.

- 범위: **±2스톱, 1/3스톱 단위** (13단계)
- 구현: LUT을 굽기 전에 로그 노광 축을 시프트. 밝기 조정으로 후처리하지 **않는다** —
  그러면 색 이동이 재현되지 않아 의미가 없다.
- 캐시: 4.1의 생성 비용 분석대로 요청 시 생성. 단계별 사전 생성 불필요.

### 4.5 그레인 (원본 01)

v1의 존별 Blend If 구조는 좋으므로 유지하고, **크기 계산만** 교체한다.

현재 `grain.js:19`의 `grainBlurRadius()`는 긴 변을 3000px로 정규화한 뒤 임의
계수 2.2를 곱한다. 해상도 독립성은 얻지만 물리적 근거가 없고, 무엇보다 **필름
포맷을 모른다**. 같은 Portra 400도 35mm과 120은 최종 이미지에서 입자 크기가 크게
다른데 v1은 이를 구분하지 못한다.

#### 새 계산

```
프레임 긴 변 (mm):
  35mm      → 36
  120 6×4.5 → 56
  120 6×6   → 56
  120 6×7   → 70
  4×5"      → 127

px_per_mm = 이미지_긴변_px / 프레임_긴변_mm
grain_px  = (effectiveGrainMicrons / 1000) × px_per_mm
```

검증 — Portra 400(유효 입자 10µm)을 6000px 긴 변으로 스캔:

| 포맷 | px/mm | 입자 크기 |
|---|---|---|
| 35mm | 166.7 | **1.67 px** |
| 120 (6×7) | 85.7 | **0.86 px** |
| 4×5" | 47.2 | **0.47 px** |

중형이 35mm의 절반, 대형이 1/3.5로 나온다. 실제 관찰과 일치한다 — 큰 포맷일수록
입자가 곱게 보이는 이유가 확대율 차이라는 물리가 그대로 재현된다.

#### 기준 해상도 — 구현은 3단계로 바꿨다

| 단계 | 해상도 | 용도 |
|---|---|---|
| **문서** | 문서 픽셀 그대로 | **기본값** |
| 표준 | 6000 | 일반 스캔·DSLR 기준으로 고정 |
| 고해상 | 9000 | 고화소·드럼스캔 기준으로 고정 |

원안은 2단계였는데 **"문서 그대로"를 표현할 방법이 없었다.** 그것이 물리적으로
옳은 기본값이다 — 위 공식이 픽셀 치수만으로 성립하므로 문서 값을 그대로 넣으면
된다. 60MP 문서에 6000px 기준을 적용하면 입자가 실제보다 곱게 나온다.

고정 기준이 필요한 경우는 따로 있다. **작업 파일과 최종 출력 크기가 다를 때**다 —
9504px로 작업하고 2048px로 웹에 올리면 축소 과정에서 입자가 뭉개져 사라진다.
그때 출력 크기를 선언해 두면 그 크기에서 맞는 입자를 만든다.

DPI 정보가 없는 파일은 **300 DPI로 가정**한다. 위 공식은 픽셀 치수와 포맷만으로
성립하므로 DPI는 인쇄 크기 안내에만 쓰이고 그레인 계산에는 직접 들어가지 않는다.
`doc.resolution`은 v1.4 실측에서 정상적으로 읽히는 것이 확인됐다.

`grain_px`가 1px 미만이면 가우시안 블러로 표현할 수 없으므로, 이 경우 블러를 끄고
노이즈 amount를 낮춰 강도로 환산한다. 픽셀보다 고운 입자는 그 자리에서 평균되어
변조 진폭이 줄어드는 것이 실제 거동이므로, 크기를 못 줄이는 대신 세기를 줄이는
편이 물리에 가깝다.

#### 할레이션에도 같은 물리가 적용된다 (원안에 없던 것)

빛이 유제와 베이스에서 산란하는 거리도 **필름면에서 고정**이다. 큰 포맷일수록
확대율이 낮아 최종 이미지에서 좁게 보인다. 그레인만 포맷을 알고 할레이션은 모르는
것은 앞뒤가 맞지 않는다.

다만 그레인처럼 절대 물리 단위로 다시 쓰지 않고 **35mm를 1.0으로 놓은 배율**로
곱한다. 할레이션 반경은 이미 긴 변 대비 %로 저장돼 해상도 독립적이었고, 35mm에서
배율이 1.0이므로 **기존 프리셋의 결과가 그대로 재현된다.** 의미를 바꾸면 저장된
프리셋이 조용히 다르게 렌더된다.

#### 구현 (v1.5, `src/format.js`)

포맷과 기준 해상도는 `params.medium`에 둔다 — 그레인 전용이 아니라 **그레인과
할레이션이 공유하는 매체 속성**이기 때문이다.

검증에서 위 표를 정확히 재현했다(35mm 1.67 / 6×7 0.86 / 4×5 0.47px). 슬라이더
0~100은 필름면 유효 입자 2~42µm에 대응하고, 기본값 20이 10µm — 이 표의 기준인
Portra 400 값이다.

구버전 프리셋은 `medium`이 없으므로 `migrate`가 35mm / 문서 기준으로 채운다.
할레이션은 배율 1.0이라 그대로 재현되고, 그레인만 물리 모델로 다시 계산된다.
**35mm 6000px에서 기존 대비 약 1.9배 굵어진다** — 기존 계산에 물리적 근거가
없었으므로 이 변화가 교정이다. 기존 룩을 유지하려면 크기 슬라이더를 낮춘다.

### 4.6 스캐너 스테이지 (원본 02) — 구현 완료

유제 단계 뒤, 사용자 조정 앞. `src/scanner.js`.

#### 왜 필요했는지 — 실측이 증명했다

유제 단계만으로 실기 테스트한 결과, 세 가지가 동시에 어긋났다.

| | 원본 | 유제만 |
|---|---|---|
| 암부 p5 | 9.3 | **24~27** (뜸) |
| 채도 | 0.420 | 0.33~0.42 (오히려 하락) |
| 필름 간 평균 R 폭 | — | **6.2** (차이 미미) |

셋 다 원인이 하나다. 실제 필름의 대비·채도는 상당 부분 인화·스캔 단계에서 나오고
블랙포인트도 스캐너가 잡는데, TDS에는 그 정보가 없다(4.2). 4.2에서 "룩의 절반
이상은 스캐너에 있다"고 적어둔 것이 수치로 확인된 셈이다.

#### 처리 순서 — 순서가 결과를 좌우한다

```
1. 레벨    블랙포인트 + 피벗 복원 감마
2. 틴트    암부 / 중간톤 / 명부 3점 보간
3. S커브   대비 (기준 그레이 피벗)
4. 채도
```

**세 번 틀리고 고쳤다. 전부 "고정점을 어디에 두는가" 문제였다.**

1. **S커브 피벗** — `v^a/(v^a+(1−v)^a)`는 0.5가 고정점인데, 필름을 통과한 사진의
   중간톤은 0.386(=0.18^(1/1.8))이라 피벗 **아래**에 있다. 그대로 쓰면 대비를
   올릴수록 중간톤이 눌린다 — 중앙값이 88.5 → **37.8**로 반토막 났다.
   → 피벗을 기준 그레이로 옮겼다(`v^k → scurve → ^(1/k)`, k = ln0.5/lnPIVOT).

2. **레벨의 피벗** — `(v−b)/(1−b)`는 상수 뺄셈이라 암부만이 아니라 중간톤도 같이
   내린다. 피벗 고정 후에도 중앙값이 63까지 떨어진 주범이었다.
   → 레벨 뒤에 감마 `ln(PIVOT)/ln((PIVOT−b)/(1−b))`를 걸어 기준 그레이를 제자리로.
   검정은 내려가고 중간톤은 유지된다.

3. **틴트 2점 → 3점** — 암부↔명부 보간만으로는 **중간톤만 물들이기**가 안 된다.
   Frontier의 "골든 스킨"이 정확히 그 특성(차가운 암부 + 따뜻한 중간톤 + 중성 명부)
   이라 중간톤 항을 따로 뒀다. 피벗 중심 삼각형 가중치, 폭 0.24 — 처음 0.45로
   잡았더니 암부까지 번져 시안 섀도를 상쇄했다.

#### 파라미터 — 측정값이 아니다

C4에서 "공개 오픈소스에서 찾아봄"이라 했으나 **쓸 만한 측정 데이터를 못 찾았다.**

- 스캐너 프로파일은 비공개.
- Negative Lab Pro 프리셋이 디스크에 있어 열어봤으나(Frontier/Pakon/Crystal/Natural
  각 22개) **읽을 수 있는 톤 파라미터가 0개**다. crs 속성 21개가 전부 메타데이터고
  실제 내용은 229,809자 LUT 블롭 하나뿐이라 비교할 수치를 뽑을 수 없었다.
- 현상소 비교 문헌은 많지만 전부 정성적.

그래서 지금 값은 **문헌의 정성 서술을 수치로 옮긴 튜닝값**이다. 코드 주석과 UI
안내문 양쪽에 명시했다. 4.6이 처음부터 "데이터 못 찾으면 눈으로 맞춘다"로
설계된 대로다.

문헌 대조에서 **내 초기 튜닝의 오류를 하나 잡았다** — Noritsu 암부를 웜으로
잡았는데, 문헌 서술은 "중성"이지 따뜻함이 아니다. "Frontier의 반대말"로 웜을
떠올리기 쉽지만 실제 대비는 시안↔중성이다.

| | Frontier | Noritsu |
|---|---|---|
| 암부 | 시안 | **중성** |
| 중간톤 | 골든 스킨 | 살짝 핑크(마젠타) |
| 명부 | 압축 (wp 0.995) | 유지 (wp 1.0) |
| 대비 | 0.12 | 0.08 |
| 채도 | 1.04 | 1.03 |

#### 실측 결과 (필름 3종 × 스캐너 3종)

```
                채도       중앙값     p5        p95       R-B
원본           0.420       88.0      9.3      235.9      11.8
없음        0.33~0.42   86~88.5    24~27    224~229     8~15
Frontier   0.533~0.536  75.6~78.1  1.9~3.6  236~240   4.1~11.1
Noritsu    0.493~0.518  79.4~81.6  5.5~8.8  232~237   9.8~15.8
```

문헌 서술과 전부 부합한다 — Frontier가 더 쨍하고(채도·대비 위) 시안 섀도(R-B 낮음)
에 검정이 더 깊고(p5 낮음), Noritsu는 뮤트하고 암부가 중성에 가깝다.

첫 튜닝은 과했다(대비 0.16 / 채도 1.12). 눈으로 보고 0.12 / 1.04로 낮춰 확정했다.

출처: [Carmencita Film Lab](https://carmencitafilmlab.com/blog/frontier-vs-noritsu-round-2/) ·
[The FIND Lab](https://thefindlab.com/2016/02/01/frontier-and-noritsu-contrast-comparison/) ·
[Richard Photo Lab](https://richardphotolab.com/blogs/post/its-back-noritsu-vs-frontier) ·
[Optik Oldschool](https://www.optik-oldschool.com/blogs/blog/why-we-prefer-frontier-scans-for-opticolour200)

### 4.7 할레이션 (유지)

`halation.js`의 현행 방식을 그대로 둔다 — 하이라이트 추출 → 채널별 블러(R이 가장
넓게) → Screen 합성. 공간 연산이라 LUT에 담을 수 없고, v1 구현의 품질에 문제가
없다. 반경을 긴 변 대비 %로 저장하는 현재 방식도 해상도 독립성을 이미 확보하고
있다.

---

## 5. 프리뷰 아키텍처

### 픽셀 취득 경로 — 검증 완료

`imaging.getPixels`는 v1의 `preview.js`에서 이미 프로덕션으로 돌고 있다.
de-risk 프로브에서도 정상 동작을 재확인했다.

**⚠ 함정 — `componentSize` 옵션을 쓰지 말 것.** 16bit 문서에서 `componentSize: 8`을
주면 `getPixels`가 `-32005 "스마트 오브젝트 파일을 업데이트할 수 없습니다"`로 죽는다.
에러 메시지가 원인과 무관해서 오진하기 쉽다 — 실제로 de-risk 프로브가 이것 때문에
두 라운드를 헛돌았다. 문서 심도 그대로 받아 JS에서 정규화한다.

**⚠ v1 버그 — `preview.js`는 16bit 문서에서 깨진다.** `gradePixels()`가 8bit를
전제하는데 16bit 문서에서는 0~65535가 들어와 전부 255로 클램프된다(미리보기가
하얗게 날아감). 평소 8bit JPEG로 작업해 드러나지 않았을 뿐이다. v2는 16bit가
전제이므로 v1.4에서 반드시 고친다. 고치는 방법은 `componentSize`가 아니라 JS 정규화다.

### 구성

```
문서 → imaging.getPixels(프록시 크기) → 사면체 LUT 적용 → 패널 표시
```

- 프록시 크기: 드래그 중 긴 변 **512px**, 손 뗀 뒤 **1024px**로 재갱신
- LUT: 33³ (프리뷰용)
- 보간: 사면체
- 스레드: **단일**. Worker 부재로 선택지 없음
- **WASM 불필요** — 순수 JS로 목표 달성 (아래 실측)

| 프록시 | 픽셀 | getPixels | getData | LUT | 합계 | 체감 |
|---|---|---|---|---|---|---|
| 512×768 | 0.39MP | 12ms | 2ms | 20ms | **34ms** | **29fps** |
| 1024×1536 | 1.57MP | 39ms | 10ms | 59ms | **108ms** | 9fps |
| 2048×3072 | 6.3MP | 167ms | 75ms | 209ms | 451ms | — |

**WASM을 뺀 근거.** 처음엔 필수라고 봤는데, 그 판단은 v1 `preview.js`의 0.63µs/px를
근거로 한 것이었다. 그 값은 `simulate.applyGrading`(커브+크로스토크+Selective Color
근사)을 픽셀마다 돌린 비용이고, 사면체 LUT 조회는 **0.033~0.051µs/px**로 10배 이상
싸다. UXP 실측치가 Node(0.046µs/px)와 같은 수준이므로 JS 엔진 차이도 없다.
512px 프록시에서 29fps가 나오므로 드래그 목표를 이미 넘긴다.

WASM은 v1.6 이후 최적화 카드로 남긴다. 지금 도입하면 툴체인만 늘고 얻는 게 없다.

### 색 팔레트 미리보기

v1의 `simulate.js`를 대체한다. LUT을 이미 굽고 있으므로, 팔레트 색 하나를
LUT에서 조회만 하면 된다. 근사가 아니라 **최종 결과와 동일한 값**이고, 연산량은
오히려 v1보다 적다.

---

## 6. 성능 목표 (F1 확정)

원본 06번의 "45MP 드래그 30fps"는 **철회한다.** Worker가 없어 단일 스레드이고,
`imaging` 왕복 비용이 있어 물리적으로 도달 불가능하다.

전부 실측으로 확정했다. 추정치가 아니다.

| 항목 | 목표 | 실측 |
|---|---|---|
| 드래그 중 프록시(512px) | 15fps | **29fps** (34ms) |
| 릴리스 후 프록시(1024px) | 300ms 이내 | **108ms** |
| LUT 재생성 (33³ / 65³) | — | **1ms / 2ms** |
| 풀해상도 적용 (60.2MP) | 20초 이내 | **3.4초** |

**풀해상도 3.4초는 Color Lookup 방식에 걸었던 원래 목표(5초)보다도 빠르다.**
GPU 가속을 잃은 것이 성능 문제로 이어지지 않았다.

60.2MP / 16bit 왕복 내역:

```
getPixels             617ms
getData               459ms
LUT 적용             2012ms   ← 59%
createImageData       178ms
putPixels             142ms
──────────────────────────────
합계                 3408ms
```

**병목은 전송이 아니라 연산이다.** 착수 전에는 반대로 봤는데, 전송 총합은 1.4초에
불과하다. 따라서 타일 분할은 속도 대책으로는 무의미하고, 피크 메모리(60MP 16bit
3채널 ≈ 360MB)가 문제될 때만 검토한다.

45MP 환산 시 약 2.5초. F1의 원래 요구("딜레이 제로화")에 실질적으로 부합한다.

최소 지원 하드웨어는 F2대로 **Windows 우선, 크로스플랫폼 가능하면 지원**.
성능이 CPU 단일 코어에 직결되므로, 저사양에서는 프록시를 512px로 고정하고
풀해상도 적용 시간만 늘어나게 한다.

---

## 7. .cube 익스포트 (원본 05) — 구현 완료 (`src/cubeexport.js`)

05번은 두 번 위상이 올라갔다.

1. Color Lookup 자동화가 막히면서(부록 B) — 사용자가 Photoshop의 Color Lookup
   조정 레이어에 *수동으로* 로드하면 우리가 자동화하지 못한 GPU 가속 비파괴
   경로를 스스로 얻을 수 있다(수동 로드 정상 동작 확인됨).
2. **ACR 프로파일 경로가 .cube를 입구로 쓴다는 것이 밝혀지면서**(§1.5) —
   .cube가 v1(픽셀 적용)과 v2(프로파일 생성)를 잇는 **다리**가 됐다.

따라서 부가 기능이 아니라 **두 갈래 산출 경로의 공통 관문**이다.

### 무엇을 굽는가

`film.buildForParams` 결과를 그대로 쓴다 — 유제 → 스캐너 → 사용자 조정까지
전부 구워진 상태다. 즉 내보낸 .cube는 "현재 문서에 적용"과 **같은 결과**를 낸다.
따로 파이프라인을 두면 두 경로가 갈라진다.

격자 33 / 65 선택. 33이 ACR·Photoshop 표준이고 65는 정밀도용.

### 색공간 — 두 벌을 내보낸다

LUT은 "입력 인코딩 → 출력 인코딩" 사상이라 **어느 인코딩 전제로 구웠는지**가
결과를 좌우한다. 엔진은 ProPhoto γ1.8에서 굽는다.

| 대상 | 공간 | 상태 |
|---|---|---|
| Photoshop Color Lookup | ProPhoto γ1.8 (문서 색공간) | 확인됨 (de-risk 수동 로드) |
| Camera Raw New Profile | ProPhoto γ1.8 | **실측 확인됨** (2026-07-25, 아래) |

**두 경로 모두 엔진 원본 공간을 그대로 쓴다. 변환이 필요 없다.**

#### 실측 (2026-07-25) — 경로 전체 성립

Agfacolor Ultra 50을 65³ / ProPhoto γ1.8로 내보내고, ACR New Profile에서 색공간
드롭다운을 **ProPhoto RGB**로 지정해 프로파일 `agfa`를 만들었다. 같은 사진에

- A: 플러그인 "현재 문서에 적용"
- B: Camera Raw 필터 + 프로파일 `agfa`

를 각각 적용하고 B를 **차이(Difference)** 블렌드로 얹었다. **결과 검정.**

즉 **엔진 → .cube → ACR 프로파일**이 픽셀 단위로 같은 결과를 낸다. 이로써

- 색공간 추측 불필요 — 드롭다운이 직접 받는다
- XMP `crs:Table_<ID>` 인코딩 해독 불필요 — Adobe 도구가 쓴다
- 같은 `.cube` 하나가 Photoshop(Color Lookup)과 Lightroom/ACR(프로파일) 양쪽에서 동작

> 판정 한계 — Difference + 레벨 증폭으로 본 육안 판정이다. 1~2 레벨 수준의
> 미세 편차는 이 방법으로 잡히지 않는다. 사면체 보간 오차(65³)는 그보다 작으므로
> 실용상 문제되지 않지만, "완전 동일"이 아니라 "판별 한계 내 일치"가 정확한 표현이다.

ACR의 New Profile 대화상자에서 Color Look-Up Table을 켜면 색공간 드롭다운이
나오고 `sRGB / Adobe RGB / P3 / ProPhoto RGB` 중에서 고른다. ProPhoto RGB를
고르면 우리가 굽는 공간과 정확히 같다.

한동안 "ACR 내부 작업 공간은 ProPhoto 원색 + sRGB 톤 응답일 것"이라는 추정으로
두 번째 선택지를 뒀다. **드롭다운의 존재로 추정 자체가 불필요해졌다** — ACR에게
직접 알려주는 것이라 내부 구현을 알 필요가 없다. 이것도 §1.5와 같은 패턴이다:
도구가 이미 물어보고 있는데 내가 답을 추측하려 했다.

### 변환 코드는 왜 남겨두는가

폴백이다. 드롭다운이 없는 경로(구버전 ACR, Profiles SDK 직접 생성)를 만날 수
있다. 지우는 것은 그런 경로가 없다고 확인한 뒤에 한다.

변환 자체는 이렇게 동작한다. 두 공간은 원색이 같고 전달함수만 달라 행렬이 필요
없다. 다만 값만 다시 인코딩하면 안 된다 — 입력 축이 여전히 옛 인코딩이라 톤이
어긋난다. **격자를 새로 깔고 값을 다시 조회**해야 한다.

```
M(y) = X→Y( L( Y→X(y) ) )
```

재조회에 보간이 들어가 원본보다 정보가 줄지만, 검증에서 같은 광량 입력에 대한
최대 선형 오차 0.00089(65격자)로 실용상 무시할 수준이다.

### 색공간을 틀렸을 때의 증상 (Portra 400 + Frontier, 65격자 실측)

| 장면 광량 | 올바름 | 틀림 | 차이 |
|---|---|---|---|
| 0.020 | 0.0041 | 0.0082 | **+1.01 스톱** |
| 0.090 | 0.0678 | 0.0814 | +0.26 |
| 0.180 | 0.1814 | 0.2018 | +0.15 |
| 0.720 | 0.7831 | 0.7687 | −0.03 |

암부만 최대 1스톱 뜨고 명부는 그대로 → **검정이 뜬 밋밋한 사진.**

18% 그레이 채널비는 더 명확하다.

| | R/G | B/G | |
|---|---|---|---|
| 올바름 | 1.023 | 0.954 | 웜 (Frontier 골든 스킨) |
| 틀림 | 0.993 | 1.057 | **쿨 (파랑)** — 색조가 뒤집힌다 |

"검정 뜨고 밋밋함"만 보면 스캐너 스테이지 미적용과 헷갈린다. **파랗게 도는 것이
색공간 오류의 고유 증상이다.**

색공간 종속성 때문에 파일에 기준을 반드시 기록한다.

```
TITLE "FilmSim Portra 400 / Fuji Frontier / 0EV"
# Color space: ProPhoto RGB (ROMM), gamma 1.8
# Generated by FilmSim
LUT_3D_SIZE 65
...
```

파일 이름에도 필름·스캐너·노광·격자·색공간을 전부 넣는다
(`Portra_400_Fuji_Frontier_+1EV_65_acr.cube`). 여러 벌 뽑으면 반드시 헷갈린다.

### 설정을 프리셋에 넣지 않는 이유

격자 크기와 대상 색공간은 **룩이 아니라 출력 형식**이다. 프리셋에 섞으면 남의
프리셋을 불러올 때마다 내보내기 설정이 같이 바뀐다. 패널 지역 상태로 둔다.

영상용 Rec.709 변형은 이 헤더 규약을 지켜두면 나중에 재샘플링만 추가하면 되므로,
지금 구조를 잡아두고 구현은 미룬다.

---

## 7.5 ACR Look 프로파일 XMP 포맷 — 규명 완료 · 구현 완료 (2026-07-25)

> 구현: `src/xmpcodec.js` (md5 / zlib / Base85), `src/xmpexport.js` (프로파일 조립).
> 재현 검증 — 같은 파라미터로 생성한 프로파일이 ACR이 만든 것과 **코너 8점 중 6점
> 정확히 일치**, 나머지 2점은 ACR 쪽 감김(아래 양자화 항목). 세트 16개 전체가
> 독립 디코더(Python)와 XML 파서를 통과했다.


`crs:Table_<ID>` 인코딩을 **실측으로 해독했다.** 우리가 만든 `agfa.xmp`를 복호해
길이·헤더·꼬리가 전부 맞아떨어지는 것을 확인했다. 이로써 `.cube`와 수동 대화상자를
거치지 않고 **플러그인이 `.xmp`를 직접 쓸 수 있다.**

### 포맷

```
binary  = <4I : 1, 1, 3, N>                     헤더 16바이트 (N = 격자 크기)
        + N³ × 3 × uint16 LE                     잔차 — 인덱스 = r·N² + g·N + b
        + <3I : 색공간, ?, 0>                     꼬리 12바이트
        + <2d : 0.0, 2.0>                        꼬리 16바이트
payload = <I : len(binary)> + zlib.compress(binary)
text    = AdobeBase85(payload)
속성    = crs:Table_<ID>="<text>"  +  crs:RGBTable="<같은 ID>"
ID      = md5(binary) 대문자 16진수 32자      ← 실측 검증됨
```

### 샘플은 값이 아니라 **항등과의 차이**다

```
stored = round((v − i/(N−1)) × 65536)  mod 65536
```

`v`는 그 격자점의 출력(0~1), `i`는 **그 채널에 대응하는 축의 인덱스**다 — R은 r,
G는 g, B는 b. 항등 LUT이면 전부 0이 된다.

잔차는 작고 매끄러워 zlib이 훨씬 잘 줄인다. ACR 테이블의 압축률이 우리 원본보다
좋았던 이유가 이것이다.

±65536은 감기면 0이 되어 **정반대 값으로 복호된다**(v=1.0·i=0 → 흰색이 검정으로,
v=0·i=N−1 → 검정이 흰색으로). 65535로 잘라 막는다. 실측 조합에서는 발생하지
않지만 가드는 둔다.

전 필름 × 전 스캐너 × 전 격자점 왕복 검증에서 최대 오차 7.6e-6(반 LSB)이다.

**축 순서가 `.cube`와 정반대다.** `.cube`는 R이 가장 빨리 변하는데(`(b·N+g)·N+r`)
여기는 **B가 가장 빨리 변하고 R이 가장 느리다**(`r·N²+g·N+b`). 그대로 옮기면
적·청이 뒤바뀐 LUT이 된다.

### 첫 판이 깨진 경위 — 검증 방법의 실패

값을 그대로 넣은 첫 판은 Lightroom에서 **밝은 영역이 파랑·자홍으로 뭉개졌다.**
암부와 중간톤은 멀쩡했다.

원인은 잔차 인코딩을 몰랐던 것인데, **왜 검증을 통과했는지가 더 중요하다.**

코너 8점으로 검증했다. 그런데 코너는 두 가지 이유로 아무것도 증명하지 못한다.

1. **잔차 인코딩에서도 값 그대로일 때와 같은 수가 나온다.** i=0이면 항등항이 0이고,
   i=N−1이면 65536 ≡ 0이다. 두 끝 다 잔차 = 값.
2. **축 순열에 불변이다.** 코너 집합은 어떤 축 순서로 읽어도 코너 집합이다.

즉 코너 일치는 잘못된 인코딩과 잘못된 축 순서를 **둘 다 통과시킨다.** 6/8 일치를
보고 포맷이 맞다고 판단한 것이 오판이었다.

어긋난 2점을 "ACR의 uint16 감김 버그"로 해석하고 65535 클램프를 넣은 것도 틀렸다.
그 0은 버그가 아니라 **정확한 잔차**였다(v=1.0, i=31 → 0).

드러난 것은 **전체 평균 오차를 재고 나서**였다 — 63.9%. 리샘플 오차로는 설명이
안 되는 값이다. 이어서 축을 따라 훑어보니 패턴이 즉시 보였다.

```
r축 차이: 0, −2113, −4221, −6336, −8450, …  ≈ −i × 2114
65536 / 31 = 2114.06
```

그 축을 따라 크게 변하는 채널만 어긋나고 나머지 두 채널은 전부 정확히 일치했다.
축 스윕 한 번이면 첫 판에 잡혔을 것이다.

> **교훈 — 일치를 확인할 때는 그 검사가 틀린 가설도 통과시키는지 먼저 따진다.**
> 대칭점(코너·원점·항등)은 여러 가설을 동시에 만족시키기 쉬워서 검증력이 없다.
> 값이 실제로 변하는 축을 따라 훑어야 한다.

잔차 인코딩 적용 후 같은 전수 비교에서 **평균차 0.0059% / 최대 35**로 떨어졌다.
남은 것은 ACR 쪽 65³→32³ 리샘플 오차뿐이다.

### 꼬리 = 색공간 (대화상자 선택이 실린다)

| 대화상자 색공간 | 꼬리 `<3I>` |
|---|---|
| ProPhoto RGB | `(2, 2, 0)` |
| sRGB | `(0, 1, 0)` |

같은 `.cube`로 두 벌을 만들어 비교한 결과 **샘플 데이터는 완전히 동일**했다. 즉
ACR은 색공간에 따라 테이블을 변환하지 않고 **메타데이터로만 기록**하고 렌더 시점에
해석한다. 우리는 ProPhoto로 굽고 `(2, 2, 0)`을 쓰면 된다.

**Adobe Base85** — Z85에서 XML 위험문자 3개만 치환한 것.

```
0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ.-:+=^!/*?`'|()[]{}@%$#
                                                                        ^^^
                                                     Z85의 & < > 를 ` ' | 로 치환
```

인코딩 방향이 함정이다. 4바이트를 **little-endian uint32**로 읽고, 85로 나눈
나머지를 **최하위 자리부터** 내보낸다. 표준 Z85(big-endian, 최상위 우선)와 반대다.

```python
x = struct.unpack('<I', data[i:i+4])[0]
for j in range(5):
    out.append(TABLE[x % 85]); x //= 85
```

### 실측 확인 (Agfacolor Ultra 50, 65³ .cube로 만든 프로파일)

```
선언 크기 196652  =  해제 결과 196652     ✓
헤더 <4I>  (1, 1, 3, 32)                  → 32³ 격자
꼬리 <3I>  (2, 2, 0)   <2d> (0.0, 2.0)
샘플 98304개 (= 32³ × 3)  범위 0 ~ 65535
계산 길이 196652 = 실제 196652            ✓
```

### **ACR은 32³로 리샘플한다**

`.cube`를 65³로 넣어도 프로파일 안에는 **32³만 남는다.** 즉 프로파일 경로에서
65³ 익스포트는 무의미하고, ACR의 다운샘플링 품질에 결과를 맡기게 된다.

**직접 생성이 품질에서도 낫다** — 엔진에서 32³을 바로 구우면 리샘플링이 한 번도
일어나지 않는다. `.cube` 왕복과 ACR 다운샘플 두 단계가 사라진다.

### 어떻게 규명했나 — 순서와 실수

1. 페이로드 문자셋이 85종(코드 33~125에서 8개 제외)임을 확인
2. ASCII85로 가정하고 디코드 → 5자 그룹의 3.19%가 2³²를 초과. 무작위 기대치와
   정확히 일치 → **알파벳 순서가 틀렸다**는 증거
3. 값 83·84는 그룹 선두에 올 수 없다는 성질로 `#`·`$` 특정. 값 82(`%`)는 드물게
   가능 → 저빈도로 관측
4. 이 세 글자 위치가 **Z85의 끝(`@%$#`)과 일치** → Z85 계열 확정
5. 관측 문자셋과 Z85의 차집합이 정확히 `&<>` ↔ `` ` ``·`'`·`|` 3쌍 → 치환 확정
6. 그런데 압축 해제 실패. 원인은 **인코딩 방향**을 표준 Z85로 가정한 것과,
   `Lir0`을 매직 헤더로 오판한 것

> `Lir0`은 헤더가 아니다. 모든 테이블의 **압축 해제 크기가 196652로 같아서**
> 그 4바이트를 인코딩한 앞 5자(`Lir00`)가 전부 같았을 뿐이다. 통계로 오프셋 4를
> 지목한 것도 이 우연이 만든 인공물이었다.

7. MIT 라이선스 구현 [CUBE-to-XMP](https://github.com/OwenWhw/CUBE-to-XMP)를 찾아
   인코딩 방향(LSD 우선 / little-endian)을 확인 → 즉시 해제 성공

### 구현에 필요한 것

| 요소 | 결정 |
|---|---|
| deflate | **zlib stored 블록.** 아래 참조 |
| Base85 | 직접 구현 (`xmpcodec.base85`) |
| md5 | 순수 JS. RFC 1321 시험벡터 5종 통과 |
| 32³ 테이블 | `film.buildForParams(params, 32)` + 축 재배열 |

### 압축을 구현하지 않은 이유

UXP에 압축 API가 없고 이 저장소에는 번들러가 없다(`package.json` 자체가 없다).
`pako`를 쓰려면 파일을 통째로 벤더링해야 한다.

대신 **DEFLATE의 stored 블록(BTYPE=00)** 을 쓴다. 규격상 완전히 유효해 어떤
해제기든 받아들이고, 30줄이면 된다. 비용은 파일 크기뿐인데 — ACR이 만든 테이블의
실제 압축률이 196652 → 177943, 즉 **1.105배**에 불과했다. 부드러운 uint16이라
애초에 잘 줄지 않는다. 무압축으로 가면 프로파일 하나가 242KB가 된다(ACR판 224KB).
16개 세트가 3.9MB. 외부 의존성 0과 맞바꿀 만하다.

크기가 문제되면 `zlibStored` 하나만 갈아끼우면 된다 — 나머지 코드는 압축 방식을
모른다.

### 직접 생성이 품질에서도 낫다

실측에서 ACR이 저장한 32³ 테이블은 **우리 엔진의 32³ 빌드와 일치하지 않았다.**
코너 8점만 정확히 맞고 내부는 달랐다. 원인은 단순하다 — 65³을 32³으로 옮길 때
두 격자가 겹치는 지점이 양 끝(i=0, i=31)뿐이고 나머지는 전부 보간이기 때문이다
(i/31×64가 정수가 되는 i는 0과 31뿐).

즉 `.cube`를 거치면 **엔진 → 65³ → ACR 리샘플 → 32³** 으로 보간이 한 번 더 낀다.
직접 생성하면 **엔진 → 32³** 한 번이다.

> 이 불일치를 처음엔 배치 오독으로 의심해 축 순서 6가지 × 채널 순서 6가지를 전수
> 대조했다. 전부 평균오차 28000대로 실패했는데, 원인이 배치가 아니라 리샘플이라
> 애초에 맞을 수가 없는 비교였다. 격자점 위치를 직접 역추적(선형 0 / 992 / 31744 /
> 32767)하고 나서야 배치가 확정됐다. **순열을 돌리기 전에 좌표 하나를 먼저 찍었어야
> 했다.**

---

## 8. 릴리스 계획 (H2/H4)

### v1.4 — LUT 엔진 전환

**목표: 엔진이 성립하는지 증명.** 성공해야 나머지가 의미를 갖는다.

- 사면체 보간 LUT 빌더
- 센시토메트리 로더 (필름 JSON 스키마)
- **putPixels 적용 경로** (getPixels → LUT → putPixels → 픽셀 레이어)
- **.cube 익스포트** (`src/cubeexport.js`) — 33³/65³, ProPhoto γ1.8 / Camera Raw
  두 색공간. 사용자 배포용이자 §1.5 프로파일 경로의 입구
- 16bit 입력 검증
- **`preview.js` 16bit 버그 수정** (5장 참조) — 16bit가 전제이므로 선결 과제
- `src/lut.js` — **작성·검증 완료** (사면체 보간, 오차 0)
- WASM **미도입** (6장 실측으로 불필요 확인)
- **필름 2종: Portra 400, Gold 200** — TDS가 가장 확실한 것부터
- `grading.js` / `simulate.js` 제거
- 스캐너 스테이지 **미포함**, 노광 보정 **미포함**

필름을 2종으로 자른 이유는, 5종을 넣으면 엔진 검증과 데이터 입력 작업이 뒤엉켜
무엇이 실패했는지 판별이 안 되기 때문이다.

### v1.5 — 광학 — 완료

- ✅ **포맷 인지 그레인** (35mm / 6×4.5 / 6×6 / 6×7 / 4×5 선택 UI) — `src/format.js`
- ✅ **기준 해상도** — 원안 2단계를 3단계로(문서 / 6000 / 9000). 4.5절 참조
- ✅ **할레이션도 포맷 인지** — 원안에 없던 것. 같은 물리라 함께 넣었다
- ✅ 스캐너 프로파일 2종 (Noritsu / Frontier) — v1.4에서 선행
- ✅ 노광 보정 (±2스톱) — v1.4에서 선행
- ✅ **도구 이분화 마무리** — 필름을 끈 상태가 "마감 모드"로 읽히게 했다.
  마감 모드에서는 8bit가 정상 입력이므로 심도 경고를 띄우지 않는다(1.5절)

### v1.6 — 확장

- **필름 3종 추가: Vision3 500T, Superia 400, Agfa Ultra**
- 실시간 프리뷰 다듬기 (드래그 스로틀·코얼레싱)
- 필요 시 WASM/SIMD 최적화 — 6장 실측상 급하지 않음
- ~~65³ 익스포트~~ — v1.4에서 완료 (§7)

### v2.0 — Lightroom / Camera Raw (연구 과제 포함)

- DCP 프로파일 생성 / 설치 (A3 확정안)
- **Camera Raw Look 프로파일 경로 재검토** — 아래 참조

#### Camera Raw 경로 — 막혔다가 풀린 경위

de-risk 과정에서 Color Lookup의 대안으로 조사했고, **구조적으로는 putPixels보다
우수하다**는 결론이 나왔다.

| | putPixels (채택) | Camera Raw Look |
|---|---|---|
| 비파괴 | ❌ 픽셀 레이어 | ✅ 스마트 필터 |
| GPU 가속 | ❌ 없음 | ✅ ACR이 담당 |
| Lightroom 공유 | ❌ 별도 작업 | ✅ 같은 .xmp가 양쪽에서 동작 |
| 구현 확실성 | ✅ 검증됨 | ✅ Adobe 변환 도구 존재 |

실측으로 확인된 것 — ACR 필터는 batchPlay로 구동 가능하고(`Adobe Camera Raw Filter`
+ `$CrVe`/`$PrVN`/`$PrVe` 버전 키 필수), 프로파일은 `$Look` 키에 **XMP 텍스트
문자열 전체**로 실린다. batchPlay가 바이너리를 거부하는 문제가 여기엔 없다.

**막혔다고 판단했던 지점** — 프로파일 XMP 안의 `crs:Table_<ID>`가 3D LUT 실데이터를
담는데 인코딩이 비공개로 보였다. 문자셋은 ASCII85 계열(고유 85자, 코드 33~125에서
XML 위험 문자 8개 제외)인데, ASCII 오름차순 알파벳으로 디코드하니 고엔트로피
바이트가 나오고 길이도 홀수라 정렬이 맞지 않았다.

**풀린 경위** — 해독할 필요가 없었다. Adobe가 .cube → 프로파일 변환을 Photoshop의
Camera Raw 안에 넣어놨고(§1.5), Profiles SDK도 공개돼 있다. 우리가 만들 것은
.cube뿐이고 그건 이미 된다(§7).

**남은 것은 해독이 아니라 색공간 확인 한 가지다.** ACR의 New Profile이 .cube를
어느 인코딩 전제로 읽는지. 두 벌 뽑아 비교하면 끝난다.

이 경로가 성립하면 **v2.0의 Lightroom 연계와 Photoshop 적용이 하나의 산출물로
합쳐진다.** DCP(A3 확정안)보다 우선순위를 높인다 — DCP는 HSV 격자라 우리 RGB
LUT을 변환해야 하고 raw 프로파일 슬롯을 차지한다.

##### 기록해 둘 판단 착오

"비공개 포맷"이라는 결론은 **페이로드를 직접 뜯어본 결과였고, 그래서 틀렸다.**
파일을 뜯기 전에 *그 회사가 만드는 도구를 먼저 찾았어야* 했다. 검색 한 번으로
나오는 것이었고, 대신 몇 시간을 디코더에 썼다.

---

## 9. 미해결 / de-risk

착수 순서대로. 앞의 것이 실패하면 뒤의 설계가 바뀐다.

**엔진 쪽 기술 리스크는 전부 종결됐다.** 남은 것은 데이터 수급뿐이다.

| # | 검증 항목 | 실패 시 영향 |
|---|---|---|
| 1 | **Agfa Ultra TDS 실존 여부** | D1 목록에서 교체. v1.6 범위라 급하지 않음 |
| 2 | Noritsu/Frontier 공개 데이터 확보 | 4.6대로 수작업 튜닝으로 대체 (일정 영향 없음) |

둘 다 실패해도 엔진은 돌아가고 일정도 밀리지 않는다. 착수를 막는 미지수는 없다.

**종결된 de-risk**

- ✅ **ACR 프로파일 경로 전체 (2026-07-25)** — 엔진 → .cube → New Profile → ACR
  적용 결과가 플러그인 픽셀 적용과 Difference 판정상 일치. 색공간은 대화상자가
  직접 물어보고(sRGB / Adobe RGB / P3 / ProPhoto RGB), ProPhoto RGB가 엔진 원본
  그대로다. **v2.0의 핵심 리스크가 여기서 종결됐다.** §7 참조

- ✅ 문서 크기·DPI·심도 읽기 — 정상
- ✅ 16bit ProPhoto 문서에서 Color Lookup 레이어 생성 — 정상
- ❌ batchPlay로 .cube 주입 — **불가 확정** (부록 B). 설계를 putPixels로 전환함
- ✅ `imaging.getPixels` / `putPixels` 왕복 — 60.2MP에서 3.4초
- ✅ 사면체 보간 정확성 — 항등 LUT 오차 0 (17³/33³/65³, 6갈래 분기 전부)
- ✅ 프리뷰 성능 — 512px에서 29fps, WASM 없이 목표 초과
- ✅ LUT 생성 비용 — 65³ 2ms, 캐시 불필요

---

## 10. 법률 (G3/G4)

### 상표

비상업 배포라도 상표권은 유효하다. 다만 위험도가 나뉜다.

- **허용 가능**: "Kodak Portra 400을 참조한 프로파일" — 지시적 언급(nominative use)
- **위험**: 로고 사용, 제품명 자체를 상표로 다는 것, 제조사 후원·인증을 암시하는 표현

**적용 방침** — 프리셋 이름은 실명 참조를 유지하되(사용자가 알아볼 수 없으면
플러그인의 의미가 없다), 다음을 넣는다.

- README와 플러그인 정보 화면에 면책 문구:
  > FilmSim은 Kodak, Fujifilm, Agfa, Noritsu와 무관한 비상업 오픈소스
  > 프로젝트입니다. 모든 상표는 각 소유자의 재산이며, 제품명은 참조 목적으로만
  > 사용됩니다.
- 제조사 로고·서체·패키지 디자인 일절 미사용
- "공식", "인증", "정품" 등의 표현 금지

### 데이터 라이선스

라이선스 확정은 개발 과정으로 미루되, **선택지를 열어두기 위한 제약**을 지금부터
적용한다.

- TDS **PDF 자체는 재배포하지 않는다.** 문서는 저작물이다.
- TDS에서 **수치를 읽어 옮긴 데이터는 사실 정보**이므로 일반적으로 배포에 문제가
  없다. 필름 JSON에 출처 문서명을 기록해 투명성을 확보한다(4.3의 `source` 필드).
- 스캐너 데이터를 외부에서 가져올 때 **GPL 또는 NC(비상업 한정) 라이선스 소스는
  피한다.** GPL 데이터를 들이면 프로젝트 전체가 GPL로 전염되고, 나중에 라이선스를
  고를 자유가 사라진다. 출처를 특정할 때마다 라이선스를 먼저 확인하고 기록한다.

---

## 부록 A. 원본 아이디어 항목

설계 논의의 출발점이 된 원본 메모. 본문에서 번호로 참조한다.

**00. 파이프라인 최적화**
- 01. raw 처리 시 Lightroom 연계 (LR 디모자이킹 엔진 활용) → **A3에서 DCP 프로파일 방식으로 확정, v2.0**
- 02. 그레이딩 이후 할레이션·그레인을 Photoshop에서 적용 → **3장 파이프라인에 반영**
- 03. 구조 추천: (1) 필름 시뮬레이션 프로파일만 LR에서 이용 (2) LR 플러그인 별도 제작
  → **(1) 채택**

**01. DPI / 해상도 가변 입자 자동 스케일링** → 4.5. 포맷 인지로 확장됨

**02. 스캐너 톤 & 프로파일 맵핑 (Noritsu & Frontier)** → 4.6. 룩의 주력으로 격상

**03. 사면체 보간 파이프라인** → 4.1. 적용 지점이 "LUT 굽기 + 프리뷰"로 이전

**04. 센시토메트리 기반 필름 에멀전 로더** → 4.2, 4.3

**05. 3D LUT (.cube) 익스포트** → 7장. 영상용은 연기, 엔진 출력 형식으로 역할 변경

**06. GPU / WebGL 렌더링 가속** → **두 번 뒤집혔다.** WebGL 부재로 네이티브
Color Lookup 레이어를 택했다가, 그마저 batchPlay로 LUT을 실을 수 없어 최종적으로
`putPixels` 직접 적용이 됐다. GPU 가속은 전부 포기했지만 **성능 목표는 오히려
초과 달성**했다(60.2MP 3.4초, 6장). 원래 명세의 "GPU 이관"은 무산됐고 "딜레이
제로화"라는 목적은 달성된 셈이다

**결론: 하이엔드/상용 유료급 지향** → G1에서 **오픈소스/비상업**으로 변경됨

---

## 부록 B. UXP 실측 로그

### 문서 픽셀 크기 / DPI 읽기 (2026-07-25, 문서 MDR02888.JPG)

✅ 통과. UXP DOM API로 모달·batchPlay 없이 즉시 읽힘.

- `doc.width` = 4160 px, `doc.height` = 6240 px, `doc.resolution` = 350 DPI
- `doc.bitsPerChannel` = bitDepth8, `doc.mode` = RGBColorMode

구현 주의: `.width`가 눈금자 단위 영향을 받을 이론적 여지가 있어 픽셀 고정
읽기로 방어한다.

### WebGL 실현 가능성 (2026-07-25)

❌ WebGL 불가 확정 / ✅ 대안(WASM + 네이티브 LUT) 확인.

- `getContext("webgl2" / "webgl" / "experimental-webgl")` 모두 null
- `WebGLRenderingContext`, `WebGL2RenderingContext`, `OffscreenCanvas` 모두 undefined
- `getContext("2d")`는 되지만 `getImageData` 없음 → 픽셀 리드백 불가
- WebAssembly ✅ 실제 실행 성공 (add 테스트 42)
- SharedArrayBuffer / Atomics ✅
- **`Worker` undefined → JS 멀티스레드 불가**
- `createImageBitmap` undefined

### Color Lookup 자동화 (2026-07-25) — ❌ 불가 확정

문서: 6336×9504 / 300DPI / **bitDepth16 / ProPhoto RGB**.

**되는 것**
- Color Lookup 조정 레이어 **생성**은 16bit ProPhoto에서 정상
- 사용자가 속성 패널에서 `.cube`를 **수동 로드**하면 정상 적용 (픽셀 `[54,52,41]` →
  `[64,59,47]` 확인)
- 우리가 생성한 33³ `.cube`(970KB)를 Photoshop이 문제없이 읽음 — **파일 포맷은 무결**

**안 되는 것 — batchPlay로 LUT 주입**

수동 로드 상태의 descriptor는 이렇다.

```jsonc
{ "_obj": "colorLookup",
  "lookupType":  { "_enum": "colorLookupType", "_value": "3DLUT" },
  "name": "…\\derisk-swap.cube",
  "dither": true,
  "LUTFormat":  { "_enum": "LUTFormatType",    "_value": "LUTFormatCUBE" },
  "dataOrder":  { "_enum": "colorLookupOrder", "_value": "rgbOrder" },
  "tableOrder": { "_enum": "colorLookupOrder", "_value": "bgrOrder" },
  "LUT3DFileData": <바이너리>,          // ← 이것만 못 넣는다
  "LUT3DFileName": "…\\derisk-swap.cube" }
```

`LUT3DFileData`를 넣는 모든 방법이 실패했다.

| 시도 | 결과 |
|---|---|
| ArrayBuffer | `Illegal data type. ByteArray is not supported` |
| `{_rawData:'base64', data:…}` | `NAPI API failure: String expected` |
| base64 문자열 | 조용히 무시 (키가 저장되지 않음) |
| `Uint8Array` | 조용히 무시 |
| 경로만 (`LUT3DFileName`) | 메타데이터는 저장되나 **파일을 읽지 않음** |

**대조 실험이 결정적이었다.** Photoshop 자신의 내장 프리셋
(`…\Presets\3DLUTs\Fuji ETERNA 250D Kodak 2395 (by Adobe).cube`) 경로를 넣어도
동일하게 무효과였다. UI로 고르면 확실히 적용되는 그 파일이다. `set`과 `make`
양쪽, 프로그램 폴더와 사용자 프리셋 폴더 양쪽 모두 실패. 즉 **우리 파일이나 경로
형식의 문제가 아니라 batchPlay의 구조적 한계**다.

### 성능 실측 (2026-07-25) — 문서 6336×9504 / 16bit / ProPhoto

사면체 보간은 `src/lut.js`, 순수 JS 단일 스레드. WASM 미사용.

**LUT 생성** — 17³ 1ms / 33³ 1ms / 65³ 2ms. 캐시 불필요.

**프리뷰 경로** (getPixels → getData → LUT 적용)

| 프록시 | 픽셀 | getPixels | getData | LUT | µs/px | 합계 |
|---|---|---|---|---|---|---|
| 512×768 | 0.39MP | 12ms | 2ms | 20ms | 0.051 | **34ms** |
| 1024×1536 | 1.57MP | 39ms | 10ms | 59ms | 0.038 | **108ms** |
| 2048×3072 | 6.3MP | 167ms | 75ms | 209ms | 0.033 | 451ms |

**풀해상도 왕복** (60.2MP)

```
getPixels 617 / getData 459 / LUT 2012 / createImageData 178 / putPixels 142
합계 3408ms
```

읽어낸 것 세 가지.

1. **병목은 연산(59%)이지 전송이 아니다.** 착수 전 예상과 반대였다. 타일 분할은
   속도 대책으로 무의미하고 메모리 대책으로만 의미가 있다.
2. **프록시 생성에 리샘플링 비용이 붙는다.** getPixels가 풀해상도에서는 10ms/MP인데
   프록시에서는 25~30ms/MP다. 작게 달라고 하는 쪽이 MP당 더 비싸다.
3. **UXP의 JS 처리량이 Node와 같은 수준이다**(0.033~0.051 vs 0.046 µs/px).
   V8 성능 차이를 걱정할 필요가 없다는 뜻이고, WASM을 뺀 근거이기도 하다.

### 사면체 보간 정확성 검증 (2026-07-25)

`src/lut.js`를 Node에서 검증. 항등 LUT으로 20,000개 난수 좌표를 조회했을 때
**17³/33³/65³ 모두 최대 오차 0** — 6갈래 분기가 전부 정확하다. swapRB LUT도
오차 0, 경계값(0/1) 정확, 범위 밖 유한, 8/16bit × 3/4채널 조합 전부 정상.

### 진단을 두 번 그르친 함정 (2026-07-25)

기록해둔다. 둘 다 에러 메시지가 원인과 무관해서 오진했다.

**1. `imaging.getPixels`에 `componentSize: 8`**
16bit 문서에서 이 옵션을 주면 `-32005 "스마트 오브젝트 파일을 업데이트할 수
없습니다"`로 죽는다. 스마트 오브젝트와 아무 관계가 없다. 이것 때문에 후보
6개가 전부 "검증 실패"로 기록돼, 주입 성공 여부 자체를 판정하지 못했다.
→ 문서 심도 그대로 받아 JS에서 정규화할 것.

**2. Photoshop의 16bit는 0~32768이다 (0~65535가 아니다)**
내부적으로 15비트+1을 쓴다. `getPixels`도 이 범위로 주고, `putPixels`에 32768을
넘기면 `invalid pixel data - 16 bit value is outside the photoshop range`로 거부한다.
8bit(0~255)에서 유추해 65535를 쓰면 **입력은 실제보다 어둡게 해석되고 출력은
범위를 넘는다.**

발견이 늦은 이유가 중요하다 — de-risk 성능 검증에 **R↔B 스왑 LUT**을 썼는데,
이런 자리바꿈 LUT은 출력이 입력 범위를 절대 넘지 않아 **오류가 드러나지 않는다.**
60.2MP 왕복이 3.4초에 성공했지만 상수는 틀린 채였다. 실제 필름 LUT은 전 구간으로
매핑하므로 첫 적용에서 바로 터졌다.
→ **검증용 LUT은 입력을 재배치만 하는 것이 아니라 값 범위를 실제로 옮기는 것을
쓸 것.** 회귀 테스트는 필름 LUT으로 전 구간을 훑어 상한 초과를 확인한다.

**3. ACR 필터 이벤트 이름 추측**
`Adobe Camera Raw Filter`가 정답이었는데도 실패했다. 이름이 아니라
`$CrVe`/`$PrVN`/`$PrVe` 버전 키를 빠뜨린 것이 원인이었고, Photoshop은
`"<알 수 없음>" 명령은 현재 사용할 수 없습니다`라는 무관한 경고를 띄웠다.
→ 이름을 추측하지 말고 **UXP 알림 리스너로 실제 실행 명령을 캡처**할 것.
`action.addNotificationListener([{event:"all"}], cb)`가 ScriptListener 역할을 한다.
이 방법으로 한 번에 정답을 얻었다.

두 번 모두 **수동으로 한 번 실행한 뒤 descriptor를 덤프하는 방법**이 추측보다
빨랐다. 앞으로 미지의 batchPlay 구조를 다룰 때의 기본 전략으로 삼는다.
