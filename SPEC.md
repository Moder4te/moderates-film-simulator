# 필름 시뮬레이션 포토샵 플러그인

## 1. 목적

디지털 사진에 아날로그 필름의 광학적·화학적 특성을 재현하는 포토샵 플러그인.
단순 LUT 적용이 아니라, 필름의 발색 원리(감색혼합), 입자 구조, 광 확산 현상을 각각 독립 파라미터로 제어하고,
그 조합을 프리셋으로 저장해 다수의 사진에 일괄 적용하는 것을 목표로 한다.

### 설계 원칙

- **비파괴 편집**: 모든 처리는 조정 레이어 / 스마트 오브젝트 기반. 원본 픽셀 미변경.
- **파라메트릭**: 결과물이 아닌 파라미터를 저장. 언제든 재조정 가능.
- **물리 기반 근사**: 각 기능은 실제 필름의 물리 현상을 근거로 모델링.

---

## 2. 기능

### 2.1 필름 스타일 컬러 그레이딩 (CMYK 기반)

RGB(가색혼합)가 아닌 CMY(감색혼합) 축으로 색을 제어한다. 필름은 시안/마젠타/옐로 3층 감광 유제의 염료 농도로 색이 결정되므로, 이 축을 그대로 노출하는 것이 필름 룩의 핵심이다.

**제어 항목**

| 항목 | 설명 |
|---|---|
| 채널별 염료 커브 | C / M / Y 각 채널의 농도 커브 (입력 농도 → 출력 농도) |
| 컬러별 CMYK 보정 | 레드·옐로·그린·시안·블루·마젠타·화이트·중간톤·블랙 9개 색역별로 C/M/Y/K 가감 |
| 크로스토크 매트릭스 | 채널 간 간섭 (필름 유제의 layer bleeding 근사) |
| 토우 / 숄더 | 암부·명부의 커브 압축 정도 (필름 특성곡선의 어깨·발끝) |
| 색온도 시프트 | 데이라이트/텅스텐 필름 밸런스 |

**구현 노트**
RGB 문서에서 CMYK 축 제어는 포토샵의 `Selective Color` 조정 레이어가 내부적으로 CMYK 좌표계를 쓰므로 이를 1차 엔진으로 사용한다. 더 정밀한 제어가 필요한 경우 픽셀 버퍼를 직접 읽어 CMY 변환 → 커브 적용 → RGB 역변환한다. 문서 모드를 실제 CMYK로 바꾸는 방식은 색역 손실이 크므로 사용하지 않는다.

---

### 2.2 명암별 차등 그레인

실제 필름 입자는 노출량에 따라 가시성이 다르다. 완전 암부와 완전 명부에서는 입자가 억제되고 중간톤에서 가장 두드러진다. 균일 노이즈로는 재현되지 않는 부분.

**제어 항목**

| 항목 | 설명 |
|---|---|
| 존별 강도 | 암부 / 중간톤 / 명부 3구간 각각의 그레인 강도 (0~100) |
| 존 경계 | 각 구간의 루미넌스 범위와 페더링 폭 |
| 입자 크기 | 그레인 스케일 (px), ISO 감도 대응 |
| 입자 형태 | 라운드 / 불규칙 (실제 은염 결정 형상 근사) |
| 컬러 그레인 | 흑백 노이즈 vs RGB 채널별 독립 노이즈 (컬러 필름은 채널별로 입자 크기 다름) |
| 해상도 독립 | 이미지 크기와 무관하게 동일한 체감 입자 크기 유지 |

**구현 노트**
루미넌스 마스크 3개를 생성해 각 구간에 별도 노이즈 레이어를 마스킹한다. 블렌드 모드는 `Overlay` 또는 `Soft Light`. 채널별 그레인은 R/G/B에 서로 다른 시드의 노이즈를 적용하되, 실제 필름 특성에 맞춰 블루 채널 입자를 가장 크게 잡는다.

---

### 2.3 할레이션 시뮬레이션

강한 광원이 필름 유제를 통과해 베이스 층에서 반사되어 되돌아오며 광원 주위에 붉은 번짐을 만드는 현상. 안티할레이션 층이 없거나 제거된 필름(예: CineStill)의 대표적 특징.

**제어 항목**

| 항목 | 설명 |
|---|---|
| 임계값 | 할레이션이 발생하는 최소 휘도 |
| 강도 | 번짐의 세기 |
| 반경 | 확산 거리 (px, 해상도 정규화) |
| 색조 | 번짐 색상 (기본 오렌지-레드, 조정 가능) |
| 채널 감쇠 | R / G / B별 확산 반경 차등 (레드가 가장 넓게 퍼짐) |
| 감쇠 곡선 | 거리에 따른 밝기 감쇠 (선형 / 지수 / 가우시안) |

**구현 노트**
파이프라인: 하이라이트 추출(임계값 마스크) → 채널별 상이한 반경의 가우시안 블러 → 색조 틴트 → `Screen` 블렌드 합성. 채널별 반경 차등이 있어야 단순 글로우가 아닌 할레이션으로 보인다.

---

### 2.4 프리셋 생성 및 저장

**기능**

- 현재 파라미터 전체를 이름 붙여 저장
- 프리셋 목록 조회 / 이름 변경 / 복제 / 삭제
- 카테고리 및 태그 분류 (예: `Portra 400`, `Vision3`, 흑백, 고대비)
- JSON 파일로 내보내기 / 가져오기 (사용자 간 공유용)
- 썸네일 미리보기 자동 생성
- 프리셋 적용 시 강도 슬라이더 (0~100%로 전체 효과 감쇠)

**데이터 구조**

```json
{
  "version": "1.0",
  "name": "Portra 400 Warm",
  "category": "Kodak",
  "tags": ["portrait", "warm", "low-contrast"],
  "grading": { "curves": {}, "selectiveColor": {}, "crosstalk": [], "toe": 0.0, "shoulder": 0.0 },
  "grain": { "shadow": 0, "midtone": 0, "highlight": 0, "size": 0.0, "colorMode": "rgb" },
  "halation": { "threshold": 0.0, "strength": 0.0, "radius": 0.0, "tint": "#FF4A1E" }
}
```

**저장 위치**
플러그인 데이터 폴더 (UXP `fs` 로컬 스토리지). 사용자 지정 폴더 내보내기 지원.

---

### 2.5 배치 적용

여러 사진에 동일 프리셋을 일괄 적용한다.

**기능**

- 입력: 폴더 선택 또는 파일 다중 선택
- 하위 폴더 포함 여부 옵션
- 출력 설정: 저장 폴더, 파일 포맷(JPEG / TIFF / PSD), 품질, 리사이즈, 파일명 규칙
- 원본 덮어쓰기 방지 (기본값: 별도 폴더 출력)
- 진행률 표시 및 중간 취소
- 실패 파일 로그 및 건너뛰기 (한 장 실패로 전체 중단되지 않음)
- 처리 전 첫 장 미리보기 확인

**구현 노트**
UXP `batchPlay`로 액션을 큐잉하고, 파일 접근은 File System Provider의 persistent token으로 폴더 권한을 유지한다. 대용량 배치는 문서를 순차적으로 열고 닫아 메모리 누적을 방지한다.

---

## 3. 기술 스택

| 항목 | 선택 |
|---|---|
| 플러그인 형식 | UXP (Photoshop 2021 이상). CEP는 지원 종료 방향이므로 제외 |
| UI | UXP Spectrum 웹 컴포넌트 + 순수 JS (빌드 단계 없음). React는 webpack/babel 툴체인이 필요해 v1 개발 루프를 느리게 만들고, 이 패널은 슬라이더 위주라 이득이 적다. UI 복잡도가 올라가면 v2에서 전환 |
| 이미지 처리 | `batchPlay` 기반 조정 레이어 조작. 정밀 연산 필요 구간만 `imaging` API로 픽셀 직접 접근 |
| 저장 포맷 | JSON |

---

## 4. 처리 순서

파이프라인 순서는 결과에 직접 영향을 준다. 아래 순서로 고정한다.

```
원본
 → 컬러 그레이딩 (CMYK)     // 색이 먼저 결정되어야 함
 → 할레이션                  // 그레이딩된 하이라이트 기준으로 확산
 → 그레인                    // 최상단. 그레인은 광학 현상이 아닌 매체 특성이므로 마지막
 → 출력
```

그레인을 그레이딩보다 먼저 적용하면 이후 색 보정에 노이즈가 함께 증폭되어 부자연스러워진다.

---

## 5. 개발 범위 구분

**v1 (핵심)**
- Selective Color 기반 CMYK 그레이딩
- 3존 그레인
- 할레이션
- 프리셋 저장 / 불러오기
- 폴더 배치 적용

**v2 (확장)**
- [x] 크로스토크 매트릭스 (Channel Mixer 기반) — 완료
- [x] 팔레트 미리보기 (패널 내 JS 시뮬레이션) — 완료
- [x] 미리보기 패널 (별도 UXP 패널, 실제 사진 imaging 렌더) — 완료
      (구 저해상도 프록시 문서 방식 → 패널 내 렌더로 대체)
- [x] 컬러휠 색 이동 시각화 (DOM translate) — 완료
- [x] 컬러휠 마커 직접 편집 (드래그 → 색역 자동 판정 + CMY) — 완료
- [x] 무채색 전용 미니휠 + 컬러/무채색 탭 — 완료
- [x] 색역 선택 커스텀 칩 (sp-picker 대체) — 완료
- [x] 더블탭 리셋 (마커·미니휠·슬라이더) — 완료
- [x] 커스텀 슬라이더 (성능: native sp-slider 대체) — 완료
- [x] 결과 레이어 그룹화 + 재적용 덮어쓰기 (v1.1) — 완료
- [x] 정식 배포 (.ccx 패키징, GitHub 릴리스) — 완료
- [x] UI 개선 (카드 레이아웃, 세로 스크롤, 위계 정리) — 완료
- [x] 특성곡선 토우·숄더 — v1에서 이미 구현됨
- [ ] 실측 필름 프로파일 번들
- [ ] 프리셋 공유 / 가져오기 UI (가져오기/내보내기는 v1에 있음)

**이 플러그인 범위에서 제외**
- 픽셀 직접 접근 기반 정밀 CMY 농도 커브 → 별도 **라이트룸 플러그인**으로 개발한다.
  CMY 3층 염료의 농도 곡선은 RAW 현상 단계(Lightroom develop 파이프라인)에 더
  맞고, LR 플러그인 SDK는 접근 방식이 달라(이 PS/UXP 플러그인과 섞지 않는다).
  Photoshop 쪽 CMY 그레이딩은 조정 레이어(Selective Color + Curves + Channel Mixer)
  근사로 유지한다.
