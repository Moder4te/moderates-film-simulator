# 남은 작업

> **성격: 동적.** 해결되면 [`docs/RESOLVED.md`](./docs/RESOLVED.md)로 옮기고 **여기서는 지운다**
> (취소선으로 남기지 않는다 — 미해결이 파묻힌다).
> 각 항목은 **맥락 없이 이것만 읽고 착수할 수 있게** 적는다: 왜 필요한지 · 어디를 건드리는지 ·
> 무엇이 되면 끝인지. 현재 상태는 [`docs/STATUS.md`](./docs/STATUS.md).

착수 전: `node tools/check.js` (문법·경계·경로·로드·함수·정합성·톤·그레인, 3초)

## 읽는 법 — **문서 순서가 곧 우선순위다**

항목 번호(`B1`·`P1`·`A1`…)는 **이력용 식별자**이지 우선순위가 아니다. 예전에는 글자
순서로 묶어 뒀는데, `B → D → C → E` 배치가 "B가 D보다 급하다"로 잘못 읽혔다 —
실제로는 B1이 재촬영 대기고 D1은 지금 바로 되는 항목이다. **이제 위에서 아래로가
급한 순서다.** 번호는 [`docs/RESOLVED.md`](./docs/RESOLVED.md)·git 이력과 대조하기
위해 그대로 둔다.

| 순 | 항목 | 비용 | 상태 |
|---|---|---|---|
| 1 | [W1 해상도 경고](#w1-해상도-경고--조용히-틀리는-마지막-경로) | 30분 | **가능** |
| 2 | [P0 할레이션 커널 형태 결정](#p0-할레이션-커널-형태-결정--p-트랙-전체를-가르는-게이트) | 반나절 | **가능. 이게 아래 둘을 정한다** |
| 3 | [P2 할레이션을 JS로](#p2-할레이션을-js로--미리보기의-지렛대) | 큼 | P0 결과에 달림 |
| 4 | [P1 마감 미리보기](#p1-마감-11-크롭-미리보기) | 중 | 가능(P2 뒤면 훨씬 단순) |
| 5 | [E2 `anchorTopLevel`](#e2-anchortoplevel의-앵커-없는-경로가-왜-동작하는지) | 프로브 1회 | **가능** |
| 6 | [D1 필름 추가](#d1-필름-추가) | 건당 소 | **가능** |
| 7 | [S1·S2 분광 데이터로 추정값 유도](#s1s2-분광-데이터로-추정값-두-개를-유도로-바꾼다) | 중 | **가능.** ⚠️추정 2건을 유도로 |
| 8 | [W2 엔진 배치](#w2-엔진에-배치가-없다) | 중 | **가능** |
| 9 | [B1 그레인 G2·G3](#b1-그레인-물리-개선-g1g5--g2g3만-남았다) | — | **재촬영 대기** |
| 10 | [R1 프리셋 마이그레이션](#r1-프리셋-마이그레이션--릴리스-게이트) · [E1 zip](#e1-build-lrpluginjs가-zip까지-만들게) | 소 | **릴리스 전 필수** |
| 11 | [E3](#e3-clearownlayers-수렴-회차-측정) · [C1](#c1-cube-폴백-색공간-정리) · [C3](#c3-영상용-rec709-cube) | 소 | 낮음 |
| 12 | [A1~A3](#9-보류--실측-대기) | — | **불가.** 두 스캐너 표본 필요 |

> **옛 번호 ↔ 새 번호** `0-3→F1` · `0-7→F2` · `0-8→F3` · `0-5b→F4`(보류) ·
> `A3(필름추가)→D1` · `A4(광도계수)→A3` · `B2→C1` · `D3→C3`. A1·A2는 그대로.
> `W1`·`W2`(워크플로) · `P0`·`P1`·`P2`(피드백 루프) · `R1`(릴리스) · `S1`·`S2`(분광)은 신규다.
> `D2`(제3자 추출본 교차검증)는 2026-08-06에 **해결돼** [`docs/RESOLVED.md`](./docs/RESOLVED.md)로 옮겼다.

---

# 1. 지금 바로

## W1. 해상도 경고 — 조용히 틀리는 마지막 경로

- **왜** 그레인 크기는 **「입자 크기 기준」 장변**에서 유도된다. `README.md`가
  *"출력 크기로 리사이즈한 뒤 마감을 적용하는 편이 간단하다. 그러지 않으려면 마감의
  '입자 크기 기준'에 최종 긴 변을 직접 지정한다"* 라고 안내하는데, **UI에는 아무
  힌트가 없다.** 안 지키면 **그레인 크기가 틀린 채로 저장되고, 결과를 봐도 잘 안
  드러난다**(작은 화면에서는 그럴듯해 보인다)
- **워크플로 점검(2026-08-04)에서 남은 유일한 "조용히 틀림"이다.** 순서 위반(색→마감)은
  v2.3.0에서 경고가 붙었다(`apps/engine/src/apply.js`) — 같은 방식을 여기에도 건다
- **덤** 이 파라미터가 격자 배열 메모리 결함이 나온 자리이기도 하다
  ([`docs/RESOLVED.md`](./docs/RESOLVED.md) "옥타브 회전이 격자 배열을 2.17배로") —
  기준을 고정값으로 두고 큰 문서를 작업하면 `cell`이 문서 크기와 무관하게 작아진다.
  경고 문구에 그 사실을 굳이 넣을 필요는 없지만, **같은 함정의 두 얼굴**이라는 것은 알아 둘 것
- **어디** `apps/finish/src/pipeline.js`의 `run` 또는 `apps/finish/src/main.js`의 적용 핸들러.
  `core/optics/format.js`의 `longEdgeFor(doc, reference, customLongEdge)`가 판정 재료다
- **완료 기준** `문서 장변 ≠ 기준 장변`이면 적용 시 상태 표시줄에 한 줄
  (막지는 않는다 — 의도적으로 다르게 두는 사용이 정상이다)

---

# 2. 결정 게이트

## P0. 할레이션 커널 형태 결정 — P 트랙 전체를 가르는 게이트

- **왜 따로 뺐나** P2를 "할까 말까"로 두면 계속 미뤄진다. **게이트만 반나절로 떼면
  결정이 강제되고**, 결과가 어느 쪽이든 P1·P2의 형태가 확정된다.
  목록 전체에서 **시간당 정보량이 가장 큰 항목**이다

- **⚠️ 물음이 바뀌었다 (2026-08-04, 선행 사례 조사).** 원래는 *"PS `maximum` 필터
  (원형 팽창)를 JS로 재현할 수 있나"*였다. 그런데 **오픈소스 할레이션 구현 중
  형태학적 팽창(disk)을 쓰는 것이 하나도 없다.**

  | 구현 | 커널 |
  |---|---|
  | [xjackyz/halation](https://github.com/xjackyz/halation) · [hotgluebanjo/halation-dctl](https://github.com/hotgluebanjo/halation-dctl) | **지수 감쇠** — *"가우시안 글로우와 달리 좁은 하이라이트→넓은 헤일로 진행이 자연스럽다"* |
  | [Nuke Halation gizmo](https://www.nukepedia.com/tools/gizmos/filter/halation/) | **Ring PSF = DoG + Lorentzian 꼬리** |
  | [spektrafilm](https://github.com/andreavolpato/spektrafilm) | 순수 가우시안 (scattering σ + halation σ 2단) |

  **전부 분리 가능한 컨볼루션이다.** 그 길로 가면 van Herk도, 팔각형 근사도,
  아래 7.6% 오차도 **통째로 사라진다.**

  그러니 물음은 이것이다 — **disk가 옳은 프리미티브인가.**
  이 프로젝트가 disk를 고른 것은 육안 관찰이고 물리적 근거도 있다
  (*"베이스 뒷면에서 튕겨 나온 빛의 궤적이라 종형이 아니라 원반"*). 하지만
  **지수 감쇠나 Lorentzian 꼬리가 "밝은 코어 + 뚜렷한 경계 + 긴 꼬리"를 더 싸게
  낼 수 있는지는 따져 본 적이 없다.**

- **방법 — 커널 세 후보를 같은 이미지에 나란히 놓는다**
  1. **현행 disk** (JS 팔각형 근사)
  2. **지수 감쇠**
  3. **DoG + Lorentzian 꼬리**

  기준은 현재 PS 출력이다. 파라미터는 같은 값으로 맞춘다.

- **참고 — disk를 JS로 갈 경우의 비용** (후보 1을 고를 때만 해당)
  - van Herk/Gil-Werman은 O(1)이지만 **직선 SE에만** 된다. 원은 분해되지 않는다
  - 실용 대안은 4방향(0/45/90/135°) **팔각형 근사** → 반경 오차 `r·(1−cos22.5°)` = **7.6%**.
    Core의 최대 팽창 반경이 100px이므로 **경계가 최대 7.6px 어긋난다**
  - 16각형이면 1.9%지만 패스가 8회. 원 마스크 직접 순회는 r=100에서 화소당 3만 회라 논외

- **완료 기준 — 넷 중 하나로 결론이 난다**
  1. **2 또는 3이 현행만큼 좋다** → 그 커널로 [P2](#p2-할레이션을-js로--미리보기의-지렛대) 진행.
     **가장 싼 결말이다** — 형태학 연산이 아예 필요 없어진다
  2. 1(팔각형 disk)이 구분 안 됨 → disk 유지한 채 P2 진행
  3. 셋 다 현행에 못 미침 → **Core만 PS에 남기는 하이브리드**로 P2 축소
     (그러면 실시간 미리보기는 못 얻는다)
  4. 하이브리드로도 안 되면 → **P2를 접고 P1(요청형)만** 간다

---

# 3. 큰 것 — 마감 피드백 루프

## P2. 할레이션을 JS로 — 미리보기의 지렛대

- **선행** [P0](#p0-할레이션-커널-형태-결정--p-트랙-전체를-가르는-게이트) 통과.

- **왜 — P1만으로는 "실시간"이 안 된다.** P1의 임시 문서 방식은 갱신 때마다 문서를
  만들고 `executeAsModal`을 돌고 닫는다. **요청형 미리보기**는 되지만 슬라이더를 끌면서
  보는 실시간은 못 된다. 엔진 미리보기가 29fps인 것은 색이 순수 JS라 문서를 아예 안
  건드리기 때문이다. 마감도 같은 조건이 되려면 **할레이션이 JS여야 한다.**

  연쇄로 열리는 것이 더 있다.
  - **그레인 합성까지 JS로** 갈 수 있게 된다 → G3(존별 클럼프)가 레이어 확대 근사가
    아니라 **픽셀별 연속 밀도 변조**로 정확해지고, G4의 교차점·가중치합=1 기계장치가
    **불필요**해진다. Blend If의 "gray ≠ 지각 휘도"(순수 빨강 34% 편차)도 사라진다
  - **마감도 노드에서 전수 검증**이 가능해진다 — P1에 "정합성 검사를 못 붙인다"고
    적어 둔 제약이 풀린다. 엔진의 `tools/check-conformance.js`와 같은 것을 마감에도 건다
  - `apps/finish/src/halation.js`의 PS 우회 코드(Maximum 100px 상한 환산 · 축소 피라미드 ·
    스케일당 레이어 복제/transform, `play()` 왕복 20회 이상)가 상당 부분 사라진다

- **어디 · 알고리즘**
  - `apps/finish/src/halation.js`의 픽셀 연산을 **`core/optics/`로 옮긴다.**
    앱에 두면 `photoshop`을 require해 노드에서 못 부른다 — `shared/paths.js`·
    `core/optics/grainzones.js`와 같은 이유다. **못 부르는 코드는 검사 없는 코드와 같다**
  - **가우시안** 박스블러 3회 근사(누적합, O(1)/px, 반경 무관). 형태 오차 ~3%
  - **최대 필터** — [P0](#p0-할레이션-커널-형태-결정--p-트랙-전체를-가르는-게이트)이
    disk를 유지하기로 할 때만 필요하다(van Herk/Gil-Werman 직선 SE × 4방향 팔각형).
    **지수 감쇠나 DoG+Lorentzian으로 가면 이 줄이 통째로 사라진다** — 전부 분리 가능한
    컨볼루션이라 박스블러와 같은 뼈대를 쓴다
  - **채널 확산차·축소 피라미드** 그대로 쓸 수 있다. 박스블러가 반경에 무관해
    피라미드는 이제 **선택사항**이 된다

- **⚠️ 한계 — 커널 결정(P0) 다음으로 큰 것부터**

  | | 내용 |
  |---|---|
  | **GPU를 잃는다** | PS 가우시안은 GPU다. JS는 단일 스레드 CPU. ⚠️ 추정 24MP 2~4초 · 60MP 5~10초 — 지금보다 **느려질 가능성이 크다.** 미리보기(크롭)는 무관, 본 적용이 문제 |
  | **메모리** | 풀해상도 float 버퍼 24MP×3ch×4B = **288MB/스케일**, 60MP면 720MB. 격자 배열 건([`RESOLVED.md`](./docs/RESOLVED.md))으로 이미 여유가 빠듯한 자리다 — **스케일 단위로 잡고 즉시 버리는 설계가 필수.** 아래 「아이디어」의 *8bit 그레인 버퍼*도 같은 축이다 |
  | **룩이 바뀐다** | 박스블러 ~3% + (disk를 유지하면) 팔각형 7.6%. 커널 자체를 바꾸면 더 크다. 프리셋 4종 전부 재튜닝([R1](#r1-프리셋-마이그레이션--릴리스-게이트)과 묶인다). G1·G4·G5에 이은 **누적**이다 |
  | **블렌드 모드 재구현** | Color Dodge·Linear Dodge·불투명도. 수식은 단순하나 PS의 클리핑·16bit 반올림이 미묘하게 다를 수 있다. 현재 출력과 1회 대조해 보정한다(일회성) |
  | **레이어 세분화 상실** | 합성까지 JS면 산출물이 한 장이 된다. **완화** — 합성된 할레이션 1장을 Screen/Add로 얹으면 "할레이션 on/off"는 유지된다. 스케일별 토글은 설계 목표로 명시된 적이 없다 |

- **⚠️ 갈림길 — 합성까지 갈 것인가**

  | | 필터만 JS | **합성까지 JS** |
  |---|---|---|
  | 안전성 | 높음(블렌드는 PS 그대로) | 재구현 위험 |
  | 실시간 미리보기 | **안 열린다** | **열린다** |
  | 레이어 세분화 | 유지 | 1장으로 합침 |

  **미리보기가 목적이면 합성까지 가야 한다.** 필터만 옮기면 P2의 가장 큰 이유가 사라진다.

  ⚠️ **경로 이중화는 이미 겪었다.** 엔진에서 미리보기와 적용이 갈라져 최대 14/255까지
  벌어진 적이 있다([`docs/RESOLVED.md`](./docs/RESOLVED.md) 0-11). 그래서 P2는 "미리보기용
  JS를 따로 쓰는" 방향이 아니라 **적용 경로까지 JS로 통일**하는 방향이다 — 경로를 둘로
  만드는 순간 같은 결함이 재발한다.

- **한계가 아닌 것** (헷갈리기 쉬워 적어 둔다)
  - **큰 반경** — 박스블러는 반경과 무관하게 O(1). Bleed 760px도 문제없다. 오히려 PS보다 유리
  - **채널 확산차** — 채널마다 반경만 다르면 된다
  - **UXP 제약** — 픽셀 왕복은 이미 검증된 경로다

- **성능이 부족하면 그때 WASM. C++ 네이티브는 조사 완료 후 접었다.**
  - **C++** 픽셀 루프는 ⚠️추정 10~20× 빨라진다. 그러나 **`core/`의 노드 검증성이 사라지고**
    (이 저장소 최대 자산) 배포가 Mac+Win 빌드 툴체인·서명·공증으로 늘고, **크래시가
    Photoshop 동반 사망**이며, PS의 C++ 플러그인에는 현대적 UI 경로가 없어 결국
    UXP 패널 + C++ 필터 + IPC로 **산출물이 둘**이 된다.
    ⚠️ **Lightroom 재사용은 착시다 — LrC SDK는 Lua라 C++ 코어를 못 붙인다.**
    **착수 조건**: ① 사용자가 실제 성능/메모리 벽을 호소 ② Mac+Win CI 감당 의지
  - **WASM(SIMD)** 이득은 ⚠️추정 2~4×로 절반이지만 **노드가 WASM을 돌리므로 검증성이
    유지되고** `.wasm` 한 장을 `.ccx`에 동봉하면 끝이다.
    [`docs/UXP-NOTES.md`](./docs/UXP-NOTES.md) 3.4의 "WASM 도입 검토했다가 불필요로
    결론냈다"는 **할레이션이 PS에 있던 시점의 결론**이므로, 이 항목이 끝나면 다시 볼
    조건이 생긴다
  - **참고 — 불안보다 여유가 있을 수 있다.** 같은 일을 하는
    [spektrafilm](https://github.com/andreavolpato/spektrafilm)(Python, 분광 시뮬레이션)이
    **6MP에 약 10초**다. 이 프로젝트는 JS로 24MP를 5초에 한다. 순수 JS가 이 도메인에서
    이미 경쟁력 있다는 뜻이므로, **P2 실측 전에 미리 가속을 결정하지 않는다**

- **완료 기준**
  1. [P0](#p0-할레이션-커널-형태-결정--p-트랙-전체를-가르는-게이트) 통과 — 이게 먼저다
  2. 할레이션 픽셀 연산이 `core/optics/`에 있고 **노드에서 돈다**
  3. 같은 파라미터로 JS 출력과 기존 PS 출력의 차이를 **수치로 재서 기록**한다
     (룩 변경이 의도된 범위인지 판단할 근거. 프리셋 재튜닝의 출발점이기도 하다)
  4. 24MP·60MP 본 적용 시간을 **실측해 [`docs/STATUS.md`](./docs/STATUS.md) 성능 표에
     적는다** — 느려졌으면 느려진 대로 적는다. WASM 판단의 근거가 된다
  5. 마감 정합성 검사(`tools/check-*`)를 붙일 수 있게 된다

## P1. 마감 1:1 크롭 미리보기

- **왜 — 피드백이 반대로 붙어 있다.**

  | | 슬라이더 | 실시간 피드백 |
  |---|---|---|
  | 엔진 | 9개 | 미리보기 · 분포 · 팔레트 · 컬러휠 |
  | 마감 | **27개** | **없음** |

  조절할 것이 3배 많은 쪽에 피드백이 0이다. 지금 마감의 유일한 루프는
  적용 → 보기 → `Ctrl+Z` → 다시 조절이고, 24MP에서 그레이딩+그레인만 5초에
  할레이션이 더 붙는다. 한 번 맞추는 데 수십 번 왕복한다.
  G4로 존 자유도가 커지면서 비용이 더 올라갔다(B1의 "특정 조합에선 어색하다").

- **⚠️ 축소 프록시로는 그레인을 보여줄 수 없다.** 이 항목의 형태를 정하는 제약이다.
  - ISO 400·장변 6000px 문서에서 grain cell ≈ 1.67px. 400px 프록시로 줄이면
    **0.11px** — 사라지거나 앨리어싱된 잡티가 된다
  - 프록시 자체 크기로 cell을 다시 계산하면 화면에는 보이지만 **내용 대비 15배 굵은
    그레인**이다. 틀린 것에 맞춰 튜닝하게 되므로 없느니만 못하다
  - → **1:1 크롭이어야 한다.** 그리고 1:1이면 그레인·디퓨전·할레이션 코어까지
    **마감 전체가 정직하게** 보인다. 못 보는 것은 전역 구도뿐이다

- **전체 프록시는 1차 범위에서 뺀다.** 할레이션은 저주파라 축소 계산이 원래 설계이고
  (축소 피라미드) 그레이딩도 프록시에서 정직하다 — 즉 "그레인만 빼면" 전체 프록시도
  가능은 하다. 그런데 마감 파라미터의 3분의 1이 그레인이라 반쪽이 된다. 구도 단위로
  보고 싶다는 요구가 실제로 생기면 그때 별도 항목으로 연다

- **어디 · 방법 — JS로 다시 짜지 않는다. 실제 파이프라인을 임시 문서에 돌린다.**

  엔진 미리보기는 색이 순수 LUT이라 JS로 됐다. **마감은 사정이 다르다.**

  | 단계 | 순수 JS인가 |
  |---|---|
  | 그레이딩 `core/optics/tone.js` | ✅ |
  | 디퓨전 `core/optics/displace.js` | ✅ |
  | 그레인 필드 `core/optics/grainfield.js` | ✅ — 단 **Overlay + Blend If 합성은 PS** |
  | 할레이션 `apps/finish/src/halation.js` | ❌ **전부 PS 필터**(gaussianBlur·maximum·blend mode·transform) |

  JS 등가물을 새로 쓰면 **구현이 둘이 된다** — `lrplugin/Info.lua`가
  "이 프로젝트에서 가장 자주 난 결함"으로 적어 둔 바로 그것이고, 엔진에서 실제로
  최대 14/255까지 갈라진 적이 있다([`docs/RESOLVED.md`](./docs/RESOLVED.md) 0-11). 그래서:

  ```
  선택 영역(또는 중앙 고정)을 임시 문서로 복제
    → pipeline.run(tempDoc, params)        ← 실제 파이프라인 그대로. 분기 없음
    → getPixels → encodeImageData → 패널 <img>
    → 임시 문서 닫기(저장 안 함)
  ```

  새 패널은 `apps/finish/manifest.json`에 추가한다(엔진의 `filmsim.preview`와 같은 방식).

- **⚠️ 이 방식으로는 "실시간"이 아니라 "요청형"이다.** 갱신마다 문서를 만들고
  `executeAsModal`을 돌고 닫는다. 슬라이더를 끌면서 보는 것은 안 된다.
  **실시간이 목표라면 [P2](#p2-할레이션을-js로--미리보기의-지렛대)가 선행되어야 한다.**
  P2를 먼저 하면 이 항목은 임시 문서 없이 크롭 버퍼 계산만으로 끝나 훨씬 단순해진다.
  P1을 먼저 해도 낭비는 아니다(요청형만으로도 지금보다 낫다) — 순서는 선택이다.

- **⚠️ 이 기능의 핵심 함정 — 스케일 두 개를 원본 기준으로 넘겨야 한다.**
  임시 문서는 작으므로, 그대로 두면 문서 크기에서 유도되는 값이 전부 틀어진다.
  - **그레인 cell** — `core/optics/format.js`의 `grainSize`가 `longEdgeFor(doc, ...)`를 쓴다.
    `medium.reference = "custom"` + `customLongEdge = 원본 장변`으로 넘긴다
  - **할레이션 반경** — `host/ps.js`의 `percentToPixels(doc, %)`가 문서 장변을 직접 쓴다.
    임시 문서에서는 원본 장변 기준으로 계산한 픽셀값을 넘겨야 한다

  **이 둘이 안 맞으면 미리보기가 실제와 다른 스케일로 나온다.** 그 상태의 미리보기는
  없는 것보다 나쁘다 — 사용자가 틀린 것을 믿고 튜닝한다. [W1](#w1-해상도-경고--조용히-틀리는-마지막-경로)과
  같은 뿌리다.

- **⚠️ 활성 문서 제약** `play()`·`stampVisible`은 **활성 문서**에 작용한다
  (`apps/engine/src/apply.js`의 배치 관련 주석이 같은 제약을 적어 뒀다). 임시 문서를
  활성으로 만들어야 하고, 그동안 사용자 문서가 비활성이 된다 — 화면 깜빡임 여부를
  실기에서 본다. 엔진 미리보기처럼 `suspendHistory`로 감싸 히스토리를 오염시키지 않는다.

- **설계는 darktable에서 빌려온다 (2026-08-04 선행 사례 조사).**
  [pixelpipe](https://deepwiki.com/darktable-org/darktable/2.1-development-context-and-pixelpipe)가
  같은 문제(비싼 파이프라인 + 상호작용 편집)의 정석 해법이다. 셋 중 하나는 이미
  이 항목의 설계와 같고, 나머지 둘은 **없어서 넣어야 한다.**

  | darktable | 여기 |
  |---|---|
  | **ROI — 화면에 보이는 픽셀만 처리** | ✅ 1:1 크롭이 곧 이것. **설계가 검증됐다** |
  | **모듈 노드별 중간 결과 캐시** | ❌ **넣는다**(아래) |
  | **상호작용 중 축소 파이프** — 드래그 중엔 비싼 모듈 제외, 놓으면 풀 품질 | ❌ **넣는다**(아래) |

  - **노드 캐시** 그레인 슬라이더만 만졌는데 그레이딩·디퓨전·할레이션을 매번 다시
    계산할 이유가 없다. 파이프가 그레이딩 → 디퓨전 → 할레이션 → 그레인 **선형 순서**라
    캐시가 특히 잘 맞는다 — 바뀐 단계보다 **앞의 결과를 재사용**하고 그 뒤만 다시 돈다
  - **상호작용 파이프** 드래그 중에는 할레이션(가장 비싼 단계)을 빼고 그레이딩+그레인만,
    손을 떼면 전체. 지금도 시각화는 rAF 코얼레싱으로 같은 발상을 쓰고 있다
    (`apps/engine/src/main.js`의 `scheduleRender`)

- **⚠️ UXP에 미리보기 선례가 없다.** 가장 가까운 오픈소스 UXP 필터 플러그인인
  [Dither-FX](https://github.com/DanielMevit/Dither-FX-1)는 픽셀 처리를 전부 JS 타입배열에서
  하고 `layerManager.js`로 PS API를 격리한다 — **`host/ps.js`와 독립적으로 같은 결론**에
  도달했다는 뜻이라 아키텍처는 검증된 셈이다. 그러나 그쪽의 "real-time"은 **200ms 디바운스로
  문서에 직접 재적용**하는 것이고 별도 미리보기 경로가 없다. 디더링은 **점 연산**이라
  그게 가능하다 — 공간 연산인 여기서는 안 된다. **P1은 실제로 새로 만들어야 한다.**

- **정합성 검사는 못 붙인다** (P2 전까지). 엔진은 `tools/check-conformance.js`가 네 산출
  경로를 노드에서 전수 대조하지만, 마감은 할레이션이 PS 필터라 **Photoshop 없이 돌릴 수
  없다.** 그래서 이 항목은 정적 검사가 아니라 **실기 검증 항목**으로 남는다. 그 사실을
  [`docs/STATUS.md`](./docs/STATUS.md)의 "실기 검증이 필요한 것"에 적는다

- **완료 기준**
  1. 크롭 미리보기가 **같은 영역을 실제로 적용한 결과와 픽셀 단위로 같다**
     (임시 문서 경로와 본 적용 경로가 같은 `pipeline.run`을 타므로 원리상 같아야 한다 —
     그것을 실기에서 한 번 확인한다)
  2. 그레인 cell·할레이션 반경이 **원본 문서 장변** 기준으로 계산된다
  3. 미리보기가 사용자 문서의 히스토리·레이어·문서 목록을 오염시키지 않는다

---

# 4. 조용한 위험

## E2. `anchorTopLevel`의 앵커 없는 경로가 왜 동작하는지

- **어디** `host/ps.js`의 `anchorTopLevel`
- **무엇** 최상위가 전부 그룹이면 앵커를 못 찾아 아무것도 하지 않는데, 실기에서는
  산출물이 **스스로 그룹 밖으로 빠져나와** 정상 동작했다. **왜 그런지 모른다**
- **왜 중요한가** 지금 그 경로가 우연히 맞는 것인지 Photoshop의 정의된 동작인지
  구분이 안 된다. 우연이면 버전이 바뀔 때 조용히 깨진다
- **방법** 프로브로 그 상태에서 `make layer` 직후 활성 레이어와 `parent`를 찍어 본다
  (v2.2.0에서 쓴 것과 같은 방식 — [`docs/UXP-NOTES.md`](./docs/UXP-NOTES.md) 2.3)
- **완료 기준** UXP-NOTES에 규칙으로 적을 수 있거나, 아니면 "만든 뒤 명시적으로 밖으로
  옮기는" 방식으로 바꾼다

---

# 5. 가치 확장

## D1. 필름 추가

**실측 대기 항목과 달리 지금 할 수 있다** — TDS가 공개돼 있다.
`tools/tds-catalog.md`에 64종을 등급별로 정리해 뒀다. A등급은 추출 완료.
다음 후보(색감이 특이한 것 우선): Vericolor III · Royal Gold 200 · Gold 200(1998) ·
Fuji Reala / Pro 400H · Agfa Optima II.

**필름 종수가 이 도구의 핵심 가치**이고 도구·데이터가 다 갖춰져 있어, 틈틈이 하기 좋다.

- **방법** `tools/extract_tds_curves.py` — 사용법과 함정 7가지는 [`tools/README.md`](./tools/README.md)
- **출력에서 반드시 볼 것** 채널 간 크기 관계(B>G>R, 리버설은 예외) · 눈금 교차 검증 ·
  γ가 상식적 범위인지 · ISO 역산이 표기값과 맞는지(Portra 400 → 399.5, Gold 200 → 207.2)
- **제3자 추출본이 있으면 대조한다** — 위 항목은 전부 자체 검증이라 같은 실수를 같은
  방식으로 하면 못 잡는다. 방법과 라이선스 주의는 [`tools/README.md`](./tools/README.md)
  "검증 방법" 4번
- **실제로 밟은 함정** 푸시 현상 곡선을 본 곡선으로 착각(Portra 800에서 γ 136) ·
  **인화지 필름을 카메라 네거티브로 오인**(UltraMax 800 = E-7024는 인화 필름. 카탈로그 오분류) ·
  x축 라벨 순서 오타 · 인접 열 라벨 누출
- **⚠️ 영화용 TDS는 레이아웃이 다르다** — 3곡선이 한 벡터 path에 연결되고 x축이 이중이라
  추출기가 그대로는 못 읽는다. Vision3 500T에서 수동 분리했다([`docs/RESOLVED.md`](./docs/RESOLVED.md) A3)
- **⚠️ 텅스텐 필름을 넣으면** `tungstenCast`를 함께 정해야 한다. 지금 값은 추정이고 실기
  눈으로 맞춘 것이다([`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) 색 응답 절)

## S1·S2. 분광 데이터로 추정값 두 개를 유도로 바꾼다

> **왜 여기 있나** — `tungstenCast`와 `crosstalk`은 [`docs/STATUS.md`](./docs/STATUS.md)
> 데이터 신뢰도에서 ⚠️추정으로 표시된 항목인데, **A1~A3과 달리 표본 대기가 아니다.**
> 필요한 데이터가 **이미 갖고 있는 TDS PDF 안에 있다.**

**배경 — 지금 모델이 무엇을 못 하는가.** `core/color/film.js`는 채널별 D-logE 3개를
쓴다. TDS 곡선은 **필름이 아니라 "필름 + 특정 측정 셋업"**이다 — Vision3 500T의 경우
`Exposure: 3200K Tungsten` · `Densitometry: ECN-2(Status M)`가 곡선에 이미 구워져 있다.
그래서 **광원을 바꾸면 어떻게 되는지 계산할 수 없고**, 손잡이를 달아 눈으로 맞춰야 했다.
분광 모델이라면 광원 SPD를 바꿔 다시 적분하면 끝나는 일이다.

**그런데 전면 분광 전환은 하지 않는다.** 4경로 정합성 보증(5e-5 이내)이 모델이
LUT-정확하기 때문에 성립하고, 데이터 확보 비용도 필름당 그래프 1장 → 4~6장이 된다.
**모델 구조는 그대로 두고 기존 필드 두 개를 실제 데이터로 채운다.**

**확인됨** — `H-1-5219t` p4에 `Spectral Sensitivity Curves`(3곡선)와
`Spectral Dye Density Curves`(시안·마젠타·옐로 + Minimum Density)가 **둘 다 있다.**
`tools/tds-catalog.md`가 이 페이지를 등급 B(플롯 영역만 지정하면 추출)로 매겨 뒀다.

### S1. 분광 감도에서 `tungstenCast` 유도

- **지금** `core/color/films.js`의 `tungstenCast {r:-0.04, g:0.08, b:0.06}` — **실기 눈으로
  맞춘 값**이다. 텅스텐 필름을 주광에서 찍었을 때의 채널별 로그노광 오프셋
- **방법** 층별 분광 감도 `S_c(λ)`를 뽑아 광원 SPD로 적분한다.
  `cast_c = log10( ∫I_daylight(λ)·10^{S_c(λ)}dλ / ∫I_3200K(λ)·10^{S_c(λ)}dλ )`
  — 즉 **설계 광원(3200K) 대비 실제 촬영 광원에서의 층별 노광량 비**
- ⚠️ **정규화 규약을 먼저 정할 것.** 전체 노광 수준은 노광 보정이 따로 처리하므로
  의미가 있는 것은 **채널 간 상대차**다. 현행 값이 어느 기준으로 잡혔는지 코드에
  기록이 없다 — 유도값과 나란히 놓고 규약을 확정한 뒤 적는다
- ⚠️ `film.js`의 계약을 지킬 것 — 캐스트는 **H에는 더하되 정규화 기준(d0/dWhite)에는
  안 더한다.** 더하면 채널 정규화가 캐스트를 중화한다([`docs/RESOLVED.md`](./docs/RESOLVED.md)
  "필름 추가(부분) — Vision3 500T")
- **완료 기준** 유도값과 현행 육안값의 차이를 수치로 적고, 어느 쪽을 쓸지 정한다.
  채택하면 `films.js` 주석의 "실기 눈으로 맞춘 값"을 근거로 교체하고 STATUS에서
  ⚠️를 뗀다. **daylight 필름 8종은 캐스트가 0이라 완전 불변** — 회귀 위험이 낮다

### S2. 분광 염료 농도에서 `crosstalk` 3×3 유도

- **지금 9종 전부 항등이다**(비대각 0) — **unwanted absorption을 아예 모델링하지 않고 있다**
- **무엇을 놓치고 있나** 실제 필름에서 채널 간 누설은 행렬이 아니라 **염료의 원치 않는
  흡수**다 — 시안 염료가 녹·청도 흡수하고, 마젠타가 청을 흡수한다. 컬러 네거티브의
  오렌지 마스크가 바로 그 보정이다. 그런데 `channelResponse`가 D₀로 정규화하면서
  **마스크는 상쇄돼 사라진다**(`film.js` 헤더). 즉 지금은 마스크를 지우고 그것이 보정하던
  누설도 함께 없는 셈이다
- **방법** 분광 염료 농도 곡선 3개를 **Status M 대역**으로 적분해 3×3을 만든다.
  대각은 주 흡수, 비대각이 unwanted absorption
- ⚠️ **적용 공간을 확인할 것.** `film.js`는 크로스토크를 **선형 포지티브 공간**에서
  3×3으로 건다(4단계 뒤). 농도 공간에서 유도한 행렬을 그대로 쓸 수 있는지,
  변환이 필요한지 먼저 정한다 — **여기를 틀리면 조용히 색이 틀어진다**
- ⚠️ **룩이 바뀐다.** 9종 전부 영향받고 항등에서 벗어나므로 정합성 검사의 색 값이
  전부 이동한다. G1·G4·G5·P2에 이은 누적이라 [R1](#r1-프리셋-마이그레이션--릴리스-게이트)과
  묶어서 본다
- **완료 기준** Vision3 500T 한 종으로 먼저 유도해 색 이동을 수치로 재고, 물리적으로
  말이 되는 방향인지(마스크가 보정하던 누설이 살아나는지) 판단한 뒤 확대 여부를 정한다

## W2. 엔진에 배치가 없다

- **왜** 마감만 폴더 배치가 된다(`apps/finish/src/batch.js`). 여러 장에 같은 필름 룩을
  주려면 `.cube`를 내보내 Color Lookup으로 수동 적용하거나 `.xmp`로 Lightroom을 거쳐야
  한다. **필름 시뮬레이터의 가장 흔한 사용(롤 한 통 일괄)이 우회 경로뿐이다**
- **어디** `apps/finish/src/batch.js`의 구조를 그대로 쓸 수 있다 — 폴더 수집 · 원본 보호
  (`shared/paths.js`의 `samePath` + `isSameFile`) · 한 장 실패해도 계속 · 순차 개폐가
  전부 앱 무관하다. 공유 층으로 올릴지 복사할지는 착수 때 정한다
- **⚠️ 활성 문서 제약** `apps/engine/src/apply.js`에 이미 적혀 있다 —
  *"배치화한다면 문서를 먼저 활성으로 만들어야 한다."* `play()`가 활성 문서에 작용한다
- **완료 기준** 폴더를 골라 필름·스캐너·노광을 일괄 적용하고, 원본 보호가
  마감 배치와 **같은 방어**(폴더 비교 + 파일 단위 `isSameFile`)를 탄다

---

# 6. 조건 대기

## B1. 그레인 물리 개선 (G1~G5) — G2·G3만 남았다

> **급하지 않아서 여기 있는 것이 아니다.** 자기 손으로 못 앞당기는 항목이라 아래에 둔다 —
> **촬영 기회가 생기면 즉시 1순위로 올라온다.** 프로토콜은 이미 확정돼 있다(아래).

- **왜** 기본 설정이 랩 스캐너 룩(휘도만, 균일)에 가깝다. 미러리스 픽셀시프트 스캔의
  실제 컬러 네거티브 그레인(채널 간 무상관·채널별 크기차·밀도별 클럼핑)을 재현하려는
  계획. 상세는 [`docs/PLAN-GRAIN-2026-08-02.md`](./docs/PLAN-GRAIN-2026-08-02.md) —
  이 문서가 최신 상태다, 여기서는 진입점만 적는다
- **완료됨(finish v2.18.0, 2026-08-03)** — G1(colorMode 기본 rgb) · G4(zoneRanges
  교차점 방식 + HIGHLIGHT_GAIN) · G5(옥타브 회전) — 셋 다 **실측치 없이** 되는
  부분이었다(물리/수학으로 결정, `tools/check-grain.js`로 값 검증). G2·G3는
  손잡이만(cellScale·ZONE_SCALE) 만들고 항등 1로 둠 — 눈에 보이는 변화 없음.
  v2.18.1에서 G4·G5의 결함 2건을 고쳤다([`docs/RESOLVED.md`](./docs/RESOLVED.md))
- **1차 실기 확인 완료(2026-08-03).** 존 슬라이더가 실제로 일한다(G4 목표
  달성) — 자유도가 높아져 특정 조합에선 어색하고, 적당히 조절하면 좋은
  이미지가 나온다. **→ CASUAL/ADVANCED 모드 분리 아이디어로 이어짐**(아래
  "아이디어" 절 — 단 **[P1](#p1-마감-11-크롭-미리보기) 이후 재평가**한다)
- **지금까지(M1)** `tools/measure-scan.js` 완성 — 의존성 0 JS, 퍼포레이션 캘리브레이션
  (피치·구멍폭·구멍높이 3중 교차검증) · 채널별 PSF · 두 프레임 센서잡음 차감 ·
  교차프레임 상관길이. **합성 데이터로 왕복 검증 완료**(88% 잡음비 조건에서도 오차
  5% 이내) — 도구 자체는 믿어도 된다. 실측 2롤(250dahu·sky/asphalt)로 G1 전제(크로마
  그레인 존재)는 확인됨
- **막힌 지점** G2(채널별 진폭·크기 상수)가 아직 안 닫힘 — 컷마다 손 초점을 다시 잡아
  크기(상관길이)를 컷 간에 못 비교하고, 진폭비의 밀도 의존성은 SNR 편향과 못 갈랐다
- **다음 재촬영에서 지킬 것** (안 지키면 또 못 씀 — 실측 타임라인에 이미 다 걸렸던 것들)
  ① **초점을 리그에 고정**하고 컷마다 다시 맞추지 않는다
  ② 밀도 단계마다 **의도적으로 평탄한 타깃**을 넣는다(맑은 하늘 조각·회색카드·미노광
  가장자리) — "아무 사진에서나 평탄한 자리 찾기"는 안 된다(자동탐색이 매번 하늘로 샘)
  ③ `--zone` 좌표는 확정 전에 반전해서 눈으로 먼저 확인한다
  ④ JPEG 경로는 쓰지 않는다(100% 품질·무편집이어도 채널간 상관이 못 쓸 정도로 틀어짐)
- **완료 기준** G2 채널별 진폭·크기 상수가 실측으로 확정되고(또는 현재 코드값
  1.23/1.0/1.76이 유지될 근거가 확정되고), `DECISIONS.md`의 "채널차는 크기가 아니라
  진폭" 결정이 유지되는지 뒤집히는지 정해진다. G1·G4·G5는 1차 실기 확인까지
  끝났다 — 남은 것은 HIGHLIGHT_GAIN 개별 체감 정도다(프리셋은 [R1](#r1-프리셋-마이그레이션--릴리스-게이트))

---

# 7. 릴리스 게이트

**배포 전에는 반드시 닫는다.** 지금은 테스트 단계라 열어 둔 것이다.

## R1. 프리셋 마이그레이션 — 릴리스 게이트

- **왜** G4에서 존 스키마가 바뀌었다(범위 3쌍 → 교차점 2개). 시드 프리셋 4종
  (`35mm Classic` · `Night Halation` · `120 Fine` · `CineStill 800T`)은 **아직 마이그레이션하지
  않았다** — 프리셋이 테스트 단계라 **의도적으로 스킵한 것**이다(제작자 판단)
- **왜 게이트인가** 프리셋은 지금 **유일한 "쉬운 진입점"**이다. 여기가 낡아 있으면
  초보 경로가 비어 있는 것과 같다
- **⚠️ [P2](#p2-할레이션을-js로--미리보기의-지렛대)와 묶인다** — P2가 들어가면 할레이션
  룩이 또 바뀌므로(박스블러 ~3% + 팔각형 7.6%) **재튜닝을 두 번 하지 않으려면 P2 뒤에 한다.**
  P2를 접기로 하면 그때 바로 진행
- **덤** `apps/finish/src/params.js`의 `migrate`가 구 키(`shadowRange`·`midtoneRange`·
  `highlightRange`)를 **무시하되 남긴다.** 다시 저장하면 계속 따라다닌다 — 함께 걷어낸다
- **완료 기준** 프리셋 4종이 새 스키마로 저장돼 있고, 각각 눈으로 확인됐다

## E1. `build-lrplugin.js`가 zip까지 만들게

- **왜** v2.2.0 릴리스에서 `FilmSim.lrplugin.zip`을 **손으로 말았다.** 매번 반복되고,
  틀리기 쉬운 자리가 하나 있다(아래)
- **⚠️ 함정 — PowerShell `Compress-Archive`를 쓰면 안 된다.** 항목 이름에 역슬래시를
  써서(`FilmSim.lrplugin\Apply.lua`) ZIP 규격을 어긴다. macOS에서 폴더가 아니라 **이름에
  역슬래시가 든 파일 하나**로 풀려 플러그인이 통째로 못 쓰게 된다.
  `README.md`가 "Windows · macOS 공통"이라고 안내하므로 조용한 사고가 된다.
  v2.2.0에서는 `bsdtar`(`C:/Windows/System32/tar.exe -a -c -f`)로 다시 말아 해결했다
- **완료 기준** `node tools/build-lrplugin.js`가 `dist/FilmSim.lrplugin.zip`까지 내고,
  항목 이름이 전부 `/`인 것을 스스로 확인한다

---

# 8. 낮음

## E3. `clearOwnLayers` 수렴 회차 측정

- **어디** `apps/finish/src/pipeline.js` · `apps/engine/src/pipeline.js`의 `guard = 16`
- **무엇** 2단 중첩까지 수렴하는 것은 확인했지만 **몇 회차에 끝나는지는 안 쟀다.**
  16의 여유가 얼마인지 모른다
- **방법** 루프에 `console.log`로 회차·대상 수를 찍고 중첩 깊이 1·2·3에서 잰다
- **완료 기준** 깊이 대비 회차 관계를 알고, 16이 어느 깊이까지 커버하는지 적는다.
  우선순위 낮음 — 실사용에서 3단 이상 중첩은 드물다

## C1. `.cube` 폴백 색공간 정리

- `core/io/cube.js`의 `acr`(ProPhoto 원색 + sRGB 톤 응답) 선택지는 **더 이상 쓸 곳이 없다.**
  New Profile 대화상자에 색공간 드롭다운이 있고 프로파일은 이제 직접 생성한다
- 드롭다운이 없는 경로(구버전 ACR, SDK 직접 생성)를 만날 가능성 때문에 폴백으로 남겨 뒀다.
  **그런 경로가 없다고 확인되면 지운다. 지금 지우면 안 된다**

## C3. 영상용 Rec.709 `.cube`

`core/io/cube.js`의 헤더 규약을 지켜뒀으므로 색공간 항목과 재샘플링만 추가하면 된다.
**우선순위 낮음** — 사진에 집중하기로 확정했다.

---

# 9. 보류 — 실측 대기

**착수 조건이 갖춰지지 않았다.** 표본이 생기면 그때 한 번에 처리한다.
그때까지 코드 주석과 [`docs/STATUS.md`](./docs/STATUS.md)에 추정값임을 명시해 둔다.

## A1. 스캐너 파라미터를 튜닝 → 측정으로

- **문제** `core/color/scanner.js`의 Frontier/Noritsu 값은 현상소 비교 문헌의 정성 서술
  ("시안 섀도 + 골든 스킨", "중성 섀도")을 수치로 옮긴 것이다. **측정값이 아니다.**
- **왜 중요한가** 필름 룩의 상당 부분이 이 단계에서 나온다([`docs/DECISIONS.md`](./docs/DECISIONS.md)
  "TDS에 있는 것과 없는 것"). 여기가 추정이면 결과물 전체가 추정이다
- **착수 조건** 둘 중 하나 — ① 공개된 스캐너 특성 데이터 확보(지금까지 못 찾음)
  ② **같은 롤을 Frontier와 Noritsu로 각각 스캔**한 표본(현상소 요청 + 그레이카드/컬러체커 한 컷)
- **하면 안 되는 것** 상용 제품(NLP 등) 프로파일에 맞춰 파라미터를 피팅. 우회 복제다
- **완료 기준** `scanner.js` 주석의 "⚠️ 실측이 아니라" 경고를 지울 수 있는 상태
- **함께 처리** A2 · A3(아래) — 셋 다 같은 표본을 쓴다

## A2. 중간 그레이 앵커 오프셋 확정

- **문제** `core/color/film.js`의 `MID_GRAY_OFFSET`이 0으로 박혀 있다
- **배경** `Log H Ref = log10(14.4/ISO)`가 Kodak 5종에서 편차 0.022스톱으로 성립함을 확인했다.
  다만 ISO 측광 이론의 18% 그레이 노광과 **약 0.25 log(0.8스톱) 어긋난다.** 검토자 두 명이
  K 상수를 달리 잡아 0.54/0.83스톱으로 다르게 계산할 만큼 값 자체가 불확실해, 추정값을 박는
  대신 손잡이만 만들어 뒀다
- **착수 조건** 기준 스캔. A1의 표본이 그 역할을 할 수 있다. 상수 **하나만** 한 번 맞춘다
- **주의** 필름마다 다른 값을 주면 안 된다. **전역 상수다** — 필름별로 넣으면 감도 차이를 이중 적용한다

## A3. 스캐너 광도 계수가 원색과 어긋남

- **문제** `core/color/scanner.js`의 `LUM`이 Rec.709 계수인데 이 단계 데이터는 **ProPhoto 원색**이다
  (ProPhoto Y행은 `[0.2880, 0.7119, 0.0001]`로 특히 청색 가중치가 크게 다르다)
- **계수만 바꿔도 "올바른 광도"가 되지 않는다.** 광도 계수는 선형광에 정의된 것인데 이 단계는
  **인코딩된 값(γ1.8)** 위에서 동작한다. 어느 계수를 쓰든 광도가 아니라 가중 평균이다
- **실측 영향** (Ektar 100) frontier 평균 0.00156 / 최대 0.0155(≈4/255) · noritsu 최대 0.0057
- **왜 보류인가** 스캐너 파라미터는 이 수식 그대로 눈으로 튜닝해 승인받은 값이다. 계수를
  바꾸면 룩이 바뀌어 재튜닝이 따라와야 하는데 그 기준이 될 실측 데이터가 없다 → **A1과 함께**

---

# 아이디어 (착수 조건 미충족)

- **CASUAL / ADVANCED 모드 분리** — ⚠️ **[P1](#p1-마감-11-크롭-미리보기) 이후 재평가한다.**
  실기 확인(2026-08-03, B1)에서 나온 아이디어다 — G4로 존 슬라이더가 실제로 일하게 되면서
  자유도가 커졌고, 특정 수치 조합에서 어색하게 보인다. **그런데 그 어색함이 "피드백이
  없어서"의 증상일 수 있다.** 보이면 스스로 교정되므로, 미리보기가 들어간 뒤에도 여전히
  필요한지 먼저 본다. 그때도 필요하면:
  ADVANCED는 지금처럼 전 슬라이더를 노출하고, CASUAL은 세부 슬라이더를 더 적은 통합
  컨트롤(예: ISO 하나)로 묶어 어색한 조합에 빠지기 어렵게 한다.
  **착수하려면 먼저 정할 것**: ① CASUAL이 실제로 대신 유도할 파라미터
  범위(존 강도만인지, feather·클럼프까지인지) ② 두 모드가 같은 프리셋 스키마를
  공유하는 표시 전환인지, 저장 형식 자체가 갈리는지 ③ 세부→통합 매핑 공식이
  임의적이지 않으려면 뭘 근거로 삼을지(육안 조정만으로는 재현성이 없다)
- **흑백 필름** — 층이 하나라 크로스토크·층별 감마가 무의미하다. RGB를 **선형 공간에서**
  하나의 노광량으로 합친 뒤 단일 곡선. `spectralWeights`가 성격을 만든다(적색 비중을 키우면
  하늘이 어두워지고 피부가 밝아진다 — 황색·적색 필터 시뮬레이션도 결국 이 가중치다)
- **8bit 그레인 버퍼** — 그레인은 Overlay 텍스처라 16bit 정밀도가 불필요하다. 출력 버퍼와
  putPixels가 절반이 된다. **선결**: 16bit 문서에 8bit putPixels가 되는지 실기 검증.
  [P2](#p2-할레이션을-js로--미리보기의-지렛대)의 메모리 항목과 같은 축이다

> **여기서 나간 것** — "마감 플러그인 미리보기"와 "C++ 하이브리드 네이티브 가속"은
> [P1](#p1-마감-11-크롭-미리보기)·[P2](#p2-할레이션을-js로--미리보기의-지렛대)로 승격됐다
> (C++은 P2 안에 "접은 근거"로 보존). 아이디어와 실작업 항목에 같은 내용을 두 벌 두지
> 않는다 — [`DOCS.md`](./DOCS.md) 갱신 규칙 1.
