# 남은 작업

> **성격: 동적.** 해결되면 [`docs/RESOLVED.md`](./docs/RESOLVED.md)로 옮기고 **여기서는 지운다**
> (취소선으로 남기지 않는다 — 미해결이 파묻힌다).
> 각 항목은 **맥락 없이 이것만 읽고 착수할 수 있게** 적는다: 왜 필요한지 · 어디를 건드리는지 ·
> 무엇이 되면 끝인지. 현재 상태는 [`docs/STATUS.md`](./docs/STATUS.md).

착수 전: `node tools/check.js` (문법·경계·로드·정합성, 3초)

## 지금 상태 — 기능은 일단락됐다

**F1~F3·F5가 v2.1.0으로, 전수 검사 항목이 v2.2.0으로 마무리됐다**(마감 그레이딩 ·
피부톤 색역 · 벡터스코프 · 할레이션 가속 · 배치 원본 보호 · 재적용 누적 · 쌓임 순서 고정).
해결 내역은 [`docs/RESOLVED.md`](./docs/RESOLVED.md), 검사 내역은
[`docs/REVIEW-2026-07-28.md`](./docs/REVIEW-2026-07-28.md).

남은 것은 성격이 넷으로 갈린다.

| | 무엇 | 착수 가능한가 |
|---|---|---|
| **[B1](#우선순위-b--그레인-물리--실측-m1-진행중)** | 그레인 물리 개선(G1~G5) | G1·G4·G5 **완료**(v2.18.0, 실기 미확인). G2·G3는 재촬영 대기 |
| **[D1](#d1-필름-추가)** | 필름 추가 | **가능.** TDS가 공개돼 있다. 틈틈이 하기 좋다 |
| **[C1·C3](#우선순위-c--정리배포)** | 정리·배포 | 가능. 범위가 작다 |
| **[E1~E3](#우선순위-e--v220에서-남긴-것)** | v2.2.0에서 남긴 것 | 가능. 전부 작다 |
| **[A1~A3](#보류--실측-대기)** | 데이터 신뢰도(색) | **불가.** 같은 롤을 두 스캐너로 스캔한 표본이 필요하다 |

> **항목 번호가 바뀌었다** (우선순위 재편). 옛 번호 ↔ 새 번호:
> `0-3→F1` · `0-7→F2` · `0-8→F3` · `0-5b→F4`(보류) · `A3(필름추가)→D1` ·
> `A4(광도계수)→A3` · `B2→C1` · `D3→C3`. A1·A2는 그대로.
> [`docs/RESOLVED.md`](./docs/RESOLVED.md)의 해결 항목은 **옛 번호를 유지**한다
> (git 이력과 대조하기 위해).

## 우선순위 B — 그레인 물리 · 실측 (M1, 진행중)

### B1. 그레인 물리 개선 (G1~G5) — G2·G3만 남았다

- **왜** 기본 설정이 랩 스캐너 룩(휘도만, 균일)에 가깝다. 미러리스 픽셀시프트 스캔의
  실제 컬러 네거티브 그레인(채널 간 무상관·채널별 크기차·밀도별 클럼핑)을 재현하려는
  계획. 상세는 [`docs/PLAN-GRAIN-2026-08-02.md`](./docs/PLAN-GRAIN-2026-08-02.md) —
  이 문서가 최신 상태다, 여기서는 진입점만 적는다
- **완료됨(finish v2.18.0, 2026-08-03)** — G1(colorMode 기본 rgb) · G4(zoneRanges
  교차점 방식 + HIGHLIGHT_GAIN) · G5(옥타브 회전) — 셋 다 **실측치 없이** 되는
  부분이었다(물리/수학으로 결정, `tools/check-grain.js`로 값 검증). G2·G3는
  손잡이만(cellScale·ZONE_SCALE) 만들고 항등 1로 둠 — 눈에 보이는 변화 없음
- **⚠️ 아직 실기(Photoshop) 미확인.** Node 검사만 통과한 상태 — 존 슬라이더·
  HIGHLIGHT_GAIN 체감·마이그레이션(구 프리셋 로드)을 실제로 열어서 봐야 한다
- **지금까지(M1)** `tools/measure-scan.js` 완성 — 의존성 0 JS, 퍼포레이션 캘리브레이션
  (피치·구멍폭·구멍높이 3중 교차검증) · 채널별 PSF · 두 프레임 센서잡음 차감 ·
  교차프레임 상관길이. **합성 데이터로 왕복 검증 완료**(88% 잡음비 조건에서도 오차
  5% 이내) — 도구 자체는 믿어도 된다. 실측 2롤(250dahu·sky/asphalt)로 G1 전제(크로마
  그레인 존재)는 확인됨
- **막힌 지점** G2(채널별 진폭·크기 상수)가 아직 안 닫힘 — 컷마다 손 초점을 다시 잡아
  크기(상관길이)를 컷 간에 못 비교하고, 진폭비의 밀도 의존성은 SNR 편향과 못 갈랐다
- **다음 재촬영에서 지킬 것** (안 지키면 또 못 씀 — 실측 타임라인에 이미 다 걸렸던 것들)
  ① **초점을 리그에 고정**하고 컷마다 다시 맞추지 않는다
  ② 밀도 단계마다 **의도적으로 평탄한 타깃**을 넣는다(맑은 하늘 조각·회색카드·미노광
  가장자리) — "아무 사진에서나 평탄한 자리 찾기"는 안 된다(자동탐색이 매번 하늘로 샘)
  ③ `--zone` 좌표는 확정 전에 반전해서 눈으로 먼저 확인한다
  ④ JPEG 경로는 쓰지 않는다(100% 품질·무편집이어도 채널간 상관이 못 쓸 정도로 틀어짐)
- **완료 기준** G2 채널별 진폭·크기 상수가 실측으로 확정되고(또는 현재 코드값
  1.23/1.0/1.76이 유지될 근거가 확정되고), `DECISIONS.md`의 "채널차는 크기가 아니라
  진폭" 결정이 유지되는지 뒤집히는지 정해진다. G1·G4·G5는 **실기 확인만 남았다**
  (위 ⚠️) — 별도로 먼저 닫아도 된다

## 우선순위 D — 데이터 확장 (지금 가능)

### D1. 필름 추가

**실측 대기 항목과 달리 지금 할 수 있다** — TDS가 공개돼 있다.
`tools/tds-catalog.md`에 64종을 등급별로 정리해 뒀다. A등급은 추출 완료.
다음 후보(색감이 특이한 것 우선): Vericolor III · Royal Gold 200 · Gold 200(1998) ·
Fuji Reala / Pro 400H · Agfa Optima II.

- **방법** `tools/extract_tds_curves.py` — 사용법과 함정 7가지는 [`tools/README.md`](./tools/README.md)
- **출력에서 반드시 볼 것** 채널 간 크기 관계(B>G>R, 리버설은 예외) · 눈금 교차 검증 ·
  γ가 상식적 범위인지 · ISO 역산이 표기값과 맞는지(Portra 400 → 399.5, Gold 200 → 207.2)
- **실제로 밟은 함정** 푸시 현상 곡선을 본 곡선으로 착각(Portra 800에서 γ 136) ·
  **인화지 필름을 카메라 네거티브로 오인**(UltraMax 800 = E-7024는 인화 필름. 카탈로그 오분류) ·
  x축 라벨 순서 오타 · 인접 열 라벨 누출
- **⚠️ 영화용 TDS는 레이아웃이 다르다** — 3곡선이 한 벡터 path에 연결되고 x축이 이중이라
  추출기가 그대로는 못 읽는다. Vision3 500T에서 수동 분리했다([`docs/RESOLVED.md`](./docs/RESOLVED.md) A3)
- **⚠️ 텅스텐 필름을 넣으면** `tungstenCast`를 함께 정해야 한다. 지금 값은 추정이고 실기
  눈으로 맞춘 것이다([`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) 색 응답 절)

## 우선순위 C — 정리·배포

### C1. `.cube` 폴백 색공간 정리

- `core/io/cube.js`의 `acr`(ProPhoto 원색 + sRGB 톤 응답) 선택지는 **더 이상 쓸 곳이 없다.**
  New Profile 대화상자에 색공간 드롭다운이 있고 프로파일은 이제 직접 생성한다
- 드롭다운이 없는 경로(구버전 ACR, SDK 직접 생성)를 만날 가능성 때문에 폴백으로 남겨 뒀다.
  **그런 경로가 없다고 확인되면 지운다. 지금 지우면 안 된다**

### C3. 영상용 Rec.709 `.cube`

`core/io/cube.js`의 헤더 규약을 지켜뒀으므로 색공간 항목과 재샘플링만 추가하면 된다.
**우선순위 낮음** — 사진에 집중하기로 확정했다.

## 우선순위 E — v2.2.0에서 남긴 것

전부 작고 지금 착수 가능하다. 급하지 않아 릴리스에서 뺐다.

### E1. `build-lrplugin.js`가 zip까지 만들게

- **왜** v2.2.0 릴리스에서 `FilmSim.lrplugin.zip`을 **손으로 말았다.** 매번 반복되고,
  틀리기 쉬운 자리가 하나 있다(아래)
- **⚠️ 함정 — PowerShell `Compress-Archive`를 쓰면 안 된다.** 항목 이름에 역슬래시를
  써서(`FilmSim.lrplugin\Apply.lua`) ZIP 규격을 어긴다. macOS에서 폴더가 아니라 **이름에
  역슬래시가 든 파일 하나**로 풀려 플러그인이 통째로 못 쓰게 된다.
  `README.md`가 "Windows · macOS 공통"이라고 안내하므로 조용한 사고가 된다.
  v2.2.0에서는 `bsdtar`(`C:/Windows/System32/tar.exe -a -c -f`)로 다시 말아 해결했다
- **완료 기준** `node tools/build-lrplugin.js`가 `dist/FilmSim.lrplugin.zip`까지 내고,
  항목 이름이 전부 `/`인 것을 스스로 확인한다

### E2. `anchorTopLevel`의 앵커 없는 경로가 왜 동작하는지

- **어디** `host/ps.js`의 `anchorTopLevel`
- **무엇** 최상위가 전부 그룹이면 앵커를 못 찾아 아무것도 하지 않는데, 실기에서는
  산출물이 **스스로 그룹 밖으로 빠져나와** 정상 동작했다. **왜 그런지 모른다**
- **왜 중요한가** 지금 그 경로가 우연히 맞는 것인지 Photoshop의 정의된 동작인지
  구분이 안 된다. 우연이면 버전이 바뀔 때 조용히 깨진다
- **방법** 프로브로 그 상태에서 `make layer` 직후 활성 레이어와 `parent`를 찍어 본다
  (v2.2.0에서 쓴 것과 같은 방식 — [`docs/UXP-NOTES.md`](./docs/UXP-NOTES.md) 2.3)
- **완료 기준** UXP-NOTES에 규칙으로 적을 수 있거나, 아니면 "만든 뒤 명시적으로 밖으로
  옮기는" 방식으로 바꾼다

### E3. `clearOwnLayers` 수렴 회차 측정

- **어디** `apps/finish/src/pipeline.js` · `apps/engine/src/pipeline.js`의 `guard = 16`
- **무엇** 2단 중첩까지 수렴하는 것은 확인했지만 **몇 회차에 끝나는지는 안 쟀다.**
  16의 여유가 얼마인지 모른다
- **방법** 루프에 `console.log`로 회차·대상 수를 찍고 중첩 깊이 1·2·3에서 잰다
- **완료 기준** 깊이 대비 회차 관계를 알고, 16이 어느 깊이까지 커버하는지 적는다.
  우선순위 낮음 — 실사용에서 3단 이상 중첩은 드물다

## 보류 — 실측 대기

**착수 조건이 갖춰지지 않았다.** 표본이 생기면 그때 한 번에 처리한다.
그때까지 코드 주석과 [`docs/STATUS.md`](./docs/STATUS.md)에 추정값임을 명시해 둔다.

### A1. 스캐너 파라미터를 튜닝 → 측정으로

- **문제** `core/color/scanner.js`의 Frontier/Noritsu 값은 현상소 비교 문헌의 정성 서술
  ("시안 섀도 + 골든 스킨", "중성 섀도")을 수치로 옮긴 것이다. **측정값이 아니다.**
- **왜 중요한가** 필름 룩의 상당 부분이 이 단계에서 나온다([`docs/DECISIONS.md`](./docs/DECISIONS.md)
  "TDS에 있는 것과 없는 것"). 여기가 추정이면 결과물 전체가 추정이다
- **착수 조건** 둘 중 하나 — ① 공개된 스캐너 특성 데이터 확보(지금까지 못 찾음)
  ② **같은 롤을 Frontier와 Noritsu로 각각 스캔**한 표본(현상소 요청 + 그레이카드/컬러체커 한 컷)
- **하면 안 되는 것** 상용 제품(NLP 등) 프로파일에 맞춰 파라미터를 피팅. 우회 복제다
- **완료 기준** `scanner.js` 주석의 "⚠️ 실측이 아니라" 경고를 지울 수 있는 상태
- **함께 처리** A2 · A3(아래) — 셋 다 같은 표본을 쓴다

### A2. 중간 그레이 앵커 오프셋 확정

- **문제** `core/color/film.js`의 `MID_GRAY_OFFSET`이 0으로 박혀 있다
- **배경** `Log H Ref = log10(14.4/ISO)`가 Kodak 5종에서 편차 0.022스톱으로 성립함을 확인했다.
  다만 ISO 측광 이론의 18% 그레이 노광과 **약 0.25 log(0.8스톱) 어긋난다.** 검토자 두 명이
  K 상수를 달리 잡아 0.54/0.83스톱으로 다르게 계산할 만큼 값 자체가 불확실해, 추정값을 박는
  대신 손잡이만 만들어 뒀다
- **착수 조건** 기준 스캔. A1의 표본이 그 역할을 할 수 있다. 상수 **하나만** 한 번 맞춘다
- **주의** 필름마다 다른 값을 주면 안 된다. **전역 상수다** — 필름별로 넣으면 감도 차이를 이중 적용한다

### A3. 스캐너 광도 계수가 원색과 어긋남

- **문제** `core/color/scanner.js`의 `LUM`이 Rec.709 계수인데 이 단계 데이터는 **ProPhoto 원색**이다
  (ProPhoto Y행은 `[0.2880, 0.7119, 0.0001]`로 특히 청색 가중치가 크게 다르다)
- **계수만 바꿔도 "올바른 광도"가 되지 않는다.** 광도 계수는 선형광에 정의된 것인데 이 단계는
  **인코딩된 값(γ1.8)** 위에서 동작한다. 어느 계수를 쓰든 광도가 아니라 가중 평균이다
- **실측 영향** (Ektar 100) frontier 평균 0.00156 / 최대 0.0155(≈4/255) · noritsu 최대 0.0057
- **왜 보류인가** 스캐너 파라미터는 이 수식 그대로 눈으로 튜닝해 승인받은 값이다. 계수를
  바꾸면 룩이 바뀌어 재튜닝이 따라와야 하는데 그 기준이 될 실측 데이터가 없다 → **A1과 함께**

## 아이디어 (착수 조건 미충족)

- **마감 플러그인 미리보기** — 보류(2026-07-28). 할레이션은 프록시에서 값싸게 되지만,
  **가장 큰 위험은 성능이 아니라 경로 이중화다.** 지금 할레이션은 batchPlay로 도는데
  미리보기를 JS로 다시 구현하면 두 경로가 갈라져 "미리보기와 결과가 다른" 문제가
  생긴다(엔진에서 겪었다 — [`docs/RESOLVED.md`](./docs/RESOLVED.md) 0-11, 최대 14/255).
  마감 플러그인에 미리보기 패널 자체도 없다. **착수하려면 먼저 정할 것**: ① 근사임을
  명시하고 감수 ② 할레이션을 순수 JS로 옮겨 적용 경로까지 통일(GPU 가우시안을 잃는다)
  ③ 정합성 검사로 두 경로 차이를 못 박기

- **C++ 하이브리드 네이티브 가속** — 조사 완료. 이득(멀티스레드·SIMD·네이티브 힙)보다
  비용(Mac+Win 빌드 툴체인, 크래시가 PS 동반 사망, 테스트성 상실)이 크다.
  **착수 조건**: ① 사용자가 실제 성능/메모리 벽 호소 ② Mac+Win CI 감당 의지.
  ⚠️ Lightroom 재사용은 착시다 — **LrC SDK는 Lua라 C++ 코어를 못 붙인다**
- **흑백 필름** — 층이 하나라 크로스토크·층별 감마가 무의미하다. RGB를 **선형 공간에서**
  하나의 노광량으로 합친 뒤 단일 곡선. `spectralWeights`가 성격을 만든다(적색 비중을 키우면
  하늘이 어두워지고 피부가 밝아진다 — 황색·적색 필터 시뮬레이션도 결국 이 가중치다)
- **8bit 그레인 버퍼** — 그레인은 Overlay 텍스처라 16bit 정밀도가 불필요하다. 출력 버퍼와
  putPixels가 절반이 된다. **선결**: 16bit 문서에 8bit putPixels가 되는지 실기 검증
