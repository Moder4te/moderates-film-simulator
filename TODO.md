# 남은 작업

각 항목은 **맥락 없이 이 문서만 읽고 착수할 수 있게** 적었다. 왜 필요한지, 어디를
건드리는지, 무엇이 되면 끝인지가 들어 있다.

현재 상태는 **v2.0.0-beta** — 플러그인이 엔진(색)과 마감(입자·산란)으로 나뉘었다.
엔진의 색 경로와 프로파일 생성은 실기 검증을 마쳤고, **분리 후 마감 쪽은 실기
검증 전이다**(우선순위 0 참조).

설계 근거는 `v2plan.md`, UXP 플랫폼 지식과 검증 방법론은 `UXP-NOTES.md`.
코드를 고쳤으면 `node tools/check.js`를 먼저 돌린다.

---

## 우선순위 0 — v2.0 실기에서 나온 것

분리 직후 실사용에서 확인된 8건. **0-1은 데이터가 파괴되는 버그**이고, **0-6은
색이 조용히 잘려나가는 정확성 문제**다. 나머지는 설계·조사 과제다.

---

### 0-1. 마감 적용 시 원본 사진이 사라진다 — **버그, 원인 규명 완료**

**증상.** 마감 플러그인만 쓰면 배경 레이어에 할레이션이 덮어써지고 원본이 없어진다.

**원인.** `host/ps.js`의 `stampVisible()`이 `{_obj:"mergeVisible", duplicate:true}`
하나만 낸다. 이것은 Photoshop의 `Ctrl+Alt+Shift+E`에 해당하는데, **그 조작은 새
빈 레이어를 먼저 만들어 둔 상태를 전제한다.** 그러지 않으면 병합 결과가 **활성
레이어 안으로 들어간다** — 활성 레이어가 배경이면 배경이 스탬프로 바뀌고, 이어지는
Levels(하이라이트 추출)가 그 위에서 돌아 원본이 사라진다.

**왜 이제야 드러났나.** v1.x에서는 `applyColor`가 먼저 픽셀 레이어를 만들고 그것을
활성 상태로 남겼다. 항상 배경이 아닌 활성 레이어가 있었기 때문에 이 결함이 가려져
있었다. 분리하면서 색 단계가 빠지자 그대로 노출됐다. **잠복 버그를 구조 변경이
드러낸 사례다.**

**방법.** `halation.apply`에서 `stampVisible` 앞에 `ps.makePixelLayer()`를 넣는다.
근본적으로는 `stampVisible()` 자체가 "빈 레이어 생성 + 병합" 두 단계를 함께 내도록
고치는 편이 안전하다 — 호출자가 전제를 기억해야 하는 API는 또 틀린다.

**완료 기준.** 배경 레이어 하나뿐인 JPEG에 마감을 적용해도 원본이 남고, 재적용해도
누적되지 않는다. 회귀를 막으려면 레이어 수를 검사하는 실기 확인 절차를 남긴다.

---

### 0-2. 입자 크기 기준에 장축 직접 입력

**현재.** `document` / `6000` / `9000` 세 가지 고정 선택지뿐이다(`core/optics/format.js`
의 `REFERENCES`).

**문제.** 최종 출력이 2048px이나 4096px이면 어느 것도 맞지 않는다. 웹 게시용
축소를 전제로 입자를 잡으려는 것이 이 기능의 목적인데 그 목적을 못 채운다.

**방법.** 숫자 입력 칸을 추가하고 `reference: "custom"` + `customLongEdge` 를 둔다.
`longEdgeFor()`만 고치면 되고 계산식은 그대로다.

**주의.** 입력값이 비었거나 비정상일 때 조용히 0이 되면 입자가 사라진다. 하한을
두고, 현재 값이 만드는 입자 크기를 px로 계속 보여준다(이미 그렇게 하고 있다).

---

### 0-3. 마감툴에 밴딩 없는 그레이딩 — **설계 필요**

**요구.** 마감툴이 독립 도구로 완결되어야 한다. 지금은 색 조정이 아예 없어서,
이미 현상된 JPEG의 톤을 조금 손보려면 엔진을 켜야 한다.

**제약이 곧 설계다.** 8bit는 채널당 256단계뿐이라 곡선을 세게 걸면 밴딩이 뜬다.
그래서 "제한된 그레이딩"이라는 요구가 나온 것이고, **무엇을 어디까지 제한할지가
이 항목의 본체다.**

밴딩을 줄이는 수단은 셋이고 성격이 다르다.

| 수단 | 효과 | 비용 |
|---|---|---|
| 조정 폭 자체를 제한 | 확실하지만 표현력이 준다 | 없음 |
| 내부 16bit 경유 후 디더링 하강 | 표현력을 지키면서 밴딩을 없앤다 | 모드 변환 왕복 |
| 그레인으로 가린다 | 이미 있는 기능이 밴딩을 덮는다 | 그레인을 꺼야 할 때 무력 |

**세 번째가 이 도구에 특히 잘 맞는다.** 마감툴은 어차피 그레인을 얹는 도구다.
그레인이 켜져 있으면 디더 역할을 하므로 밴딩 임계가 크게 올라간다. 다만 그레인
강도 0에서도 성립해야 하므로 이것만으로 끝낼 수는 없다.

**권장 조합** — 두 번째를 기본으로 하고, 조정 폭에는 안전 상한만 둔다. 8bit 문서를
받으면 내부에서 16bit로 올려 조정하고 디더링해 내린다. Photoshop의 모드 변환은
비파괴가 아니므로 **원본을 건드리지 않는 경로**(복제 → 변환 → 조정 → 병합)를
설계해야 한다.

**결정해야 할 것.** 어떤 조정 항목을 넣을지. 엔진의 Selective Color 전체를 가져오면
"그레이딩이 빠진 마감툴"이라는 분리 취지가 흐려진다. 톤 커브 + 색온도 정도로
좁히는 편이 역할 구분에 맞다고 본다.

**⚠️ 경계 검사와 충돌한다.** `tools/check-boundaries.js`는 마감 앱이 `core/color`를
참조하면 실패시킨다. 그레이딩을 넣으려면 **그 규칙을 무르는 것이 아니라**, 마감용
톤 조정을 `core/optics` 쪽 순수 모듈로 따로 두어 필름 색 로직과 섞이지 않게 한다.
규칙을 예외 처리하기 시작하면 분리의 의미가 없어진다.

---

### 0-4. 그레인 수치를 감도(ISO)로 — **설계 필요**

**문제.** 입자 크기는 이미 포맷과 장축에서 물리적으로 나온다. 그런데 크기 슬라이더가
따로 있어 두 근거가 겹친다. 사용자가 "20"이 무슨 뜻인지 알 수 없다.

**방법.** 크기 슬라이더를 **필름 감도(ISO)** 로 바꾼다. 감도가 곧 입자 크기이므로
근거가 하나로 합쳐지고, 사진가에게 익숙한 단위가 된다.

```
ISO  50   → 미세립     약 4µm
ISO 100   →            약 6µm
ISO 400   → 표준       약 10µm   (현재 기본값과 같은 지점)
ISO 800   →            약 14µm
ISO 3200  → 거친 입자  약 25µm
```

로그 스케일이라 슬라이더는 log2(ISO)에 선형으로 두는 것이 자연스럽다.

**미확인.** 위 대응은 개략적인 값이지 측정치가 아니다. Kodak의 현행 TDS는 입자
지표로 PGI를 쓰는데 구형 RMS granularity와 **변환 관계가 없다고 명시**돼 있어
(README 현재 한계 참조) 공개 자료에서 곧바로 얻기 어렵다. 넣을 때 추정값임을
표시할 것.

**함께 볼 것.** 존별 강도(암부·중간·명부)는 남긴다. 그것은 크기가 아니라 가시성이고
감도로 대체되지 않는다.

---

### 0-5. 할레이션 실시간 반영 — **조사 결과: 가능, 프록시 방식**

**현재.** 미리보기 패널은 LUT(색)만 반영한다. 할레이션·그레인은 공간 연산이라
제외했다(README 핵심 구현 결정).

**가능성.** 할레이션은 **하이라이트 추출 → 채널별 가우시안 블러 → Screen 합성**이다.
전부 프록시에서 값싸게 된다.

- 미리보기 프록시는 300px다. 실제 반경이 긴 변의 1.2%이므로 프록시에서는 **3~4px**.
- 그 정도 반경의 분리 가능 가우시안은 300×300에서 수 ms다.
- 이미 픽셀 순회 렌더가 있으므로(280px에서 ~85ms) 예산 안에 들어간다.

**주의.** 프록시 반경을 실제와 같은 **비율**로 잡아야 한다. 절대 px로 옮기면
미리보기와 결과가 어긋난다 — `percentToPixels`가 이미 비율 기반이라 프록시 크기만
넘기면 된다.

**그레인은 다르다.** 입자가 프록시에서 1px 미만이 되어 의미 있는 표시가 안 된다.
할레이션만 넣고 그레인은 제외하는 편이 정직하다.

**전제.** 이 기능은 마감 플러그인에 미리보기 패널이 필요하다는 뜻이다. 지금은 없다.

---

### 0-6. 컬러휠이 색공간을 sRGB로 제한한다 — **확인됨, 심각**

**사용자 추정이 맞았다. 그리고 예상보다 크다.**

`core/color/film.js`의 `bakeGrading`이 이렇게 동작한다.

```
ProPhoto → sRGB → 그레이딩 → ProPhoto
```

`proPhotoToSrgb`가 `srgbEncode`로 [0,1]에 클램프하므로, **sRGB 색역 밖의 색은
그 자리에서 잘린다.** 그레이딩이 켜져 있으면 손대지 않은 색까지 전부 왕복한다.

**실측** (Ektar 100 + Frontier, 33³ = 35,937 격자점):

```
1/255 이상 어긋난 격자점   29,663 (82.5%)
최대 이동                  0.658 (인코딩 값)

예시 (원래 → 왕복 후)
  0.151, 0.006, 0.000  →  0.157, 0.062, 0.018
  0.274, 0.005, 0.000  →  0.286, 0.112, 0.042
```

포화 적색에서 G·B가 크게 들려 **채도가 무너진다.** ProPhoto로 작업하는 의미가
그레이딩을 켜는 순간 사라진다.

**왜 이렇게 됐나.** `simulate.js`의 색역 판정(레드/옐로/…)이 sRGB 원색을 전제로
맞춰져 있다. ProPhoto는 색역이 훨씬 넓고 일부가 가상색이라 같은 인코딩 값이라도
색상이 다르게 잡힌다. 그래서 판정을 위해 sRGB로 옮겼는데, **판정만 하면 될 것을
적용까지 그 공간에서 하고 되돌린 것**이 문제다.

**방법.** 판정과 적용을 분리한다.

1. 색역 판정은 sRGB 좌표로 계속한다 (기존 임계값을 그대로 쓸 수 있다)
2. **적용은 작업 공간에서 한다.** 클램프 없이
3. 판정용 변환에서는 클램프하지 않고 색역 밖 값을 그대로 흘려 분류만 한다

더 나은 대안은 판정을 색상각(HSL/LCh) 기준으로 옮기는 것인데, 임계값을 전부 다시
잡아야 하므로 위 방법을 먼저 시도한다.

**완료 기준.** 위 실측을 다시 돌려 왕복 이동이 사라지는 것. **정합성 검사에 이
측정을 넣어 회귀를 막는다.**

---

### 0-7. 피부톤 선택 옵션

**요구.** 그레이딩에서 피부톤을 따로 고르고 싶다.

**현재.** Selective Color의 고정 9색역뿐이라 피부는 reds와 yellows에 걸쳐 있다.
한쪽만 만지면 부자연스럽고, 양쪽을 만지면 피부가 아닌 것까지 딸려 온다.

**방법.** 색상각 + 채도 범위로 정의한 **의사 색역**을 하나 추가한다. 피부는 색상
대역이 좁고(대략 주황 15~35°) 채도 상한이 있다는 점에서 기존 색역보다 오히려
정의하기 쉽다.

**이미 있는 재료.** `photoanalysis.js`가 사진에서 색역별 대표색을 뽑는데, 여기에
스킨 대표색이 이미 나온다(실측 예: 175,143,122). 그 분포를 근거로 범위를 잡으면
임의값이 되지 않는다.

**주의.** 피부색은 인종에 따라 명도·채도 범위가 넓다. 좁게 잡으면 특정 피부만
잡히고 나머지는 반응하지 않는다. **밝기 범위는 넓게, 색상각은 좁게** 잡는 것이
안전하다. 대표색 하나에 맞추면 반드시 편향된다.

---

### 0-8. 실시간 벡터스코프 — **조사 결과: 제한적으로 가능**

**제약.** UXP에서 canvas가 블랙아웃되고 SVG·rotate도 못 쓴다(UXP-NOTES 5, 컬러휠
개발에서 확인). **DOM + translate만 가능하다.**

**그래서 점을 하나씩 찍는 방식은 안 된다.** 픽셀 수만큼 div를 만들 수 없다.

**가능한 형태.** 밀도 격자.

- 프록시(200px)를 읽어 각 픽셀의 색차 좌표(U·V 또는 a·b)를 구한다
- 원형 영역을 **64×64 정도 격자**로 나눠 도수를 센다
- 도수가 0이 아닌 칸만 div로 그리고 밝기·불투명도로 도수를 표현한다

div 수는 실제 분포가 차지하는 칸에 비례하므로 보통 수백 개 수준이다. 컬러휠이
이미 색점 72개 + 마커·경로를 DOM으로 그리고 있으므로 같은 방식을 확장하면 된다.

**성능.** 픽셀 순회는 이미 하고 있고(사진 분석이 200px 1회 순회), 격자 누적은 그
순회에 얹으면 추가 비용이 거의 없다. 갱신은 디바운스로 묶는다.

**한계를 미리 적어 둘 것.** 방송용 벡터스코프처럼 정밀한 계측이 아니라 **분포의
모양을 보는 용도**다. 격자 해상도만큼 뭉개지고, 프록시라 소수 픽셀의 극단값은
사라진다. 그렇게 표시할 것.

---

## 우선순위 A — 데이터 신뢰도

지금 이 프로젝트에서 가장 약한 고리다. 코드가 아니라 **입력 데이터**가 문제다.

### A1. 스캐너 파라미터를 튜닝 → 측정으로

**문제.** `core/color/scanner.js`의 Frontier / Noritsu 값은 현상소 비교 문헌의 정성
서술("시안 섀도 + 골든 스킨", "중성 섀도, light and airy")을 수치로 옮긴 것이다.
측정값이 아니다. 코드 주석과 UI 설명에 그렇게 표시해 뒀다.

**왜 중요한가.** 실기 측정에서 드러났듯 **필름 룩의 상당 부분이 이 단계에서 나온다.**
유제 단계만으로는 암부가 뜨고 채도가 낮고 필름 간 차이가 작다(원본 p5 9.3 → 필름
21~27). 여기가 추정값이면 결과물 전체가 추정값이다.

**할 일.** 둘 중 하나.

1. 공개된 스캐너 특성 데이터를 찾는다 (v2plan §9 미해결 2번). 지금까지 못 찾았다.
2. **같은 롤을 Frontier와 Noritsu로 각각 스캔**한 표본을 확보한다. 현상소에
   요청하면 된다. 그레이카드나 컬러체커를 한 컷 넣으면 정량 비교가 가능하다.

**하면 안 되는 것.** 상용 제품(NLP 등)의 스캐너 프로파일에 맞춰 파라미터를
피팅하는 것. 우회 복제다. `UXP-NOTES.md` 9.3 참고.

**완료 기준.** `core/color/scanner.js` 주석의 "⚠️ 실측이 아니라" 경고를 지울 수 있는 상태.

### A2. 중간 그레이 앵커 오프셋 확정

**문제.** `core/color/film.js`의 `MID_GRAY_OFFSET`이 0으로 박혀 있다.

Kodak 5종 대조로 `Log H Ref = log10(14.4 / ISO)`가 편차 0.022스톱으로 성립함을
확인했다. 즉 Log H Ref는 감도로 완전히 결정되는 기준 노광점이다. 다만 ISO 측광
이론의 18% 그레이 노광(H = q·K/S, 약 8.1/S)과는 **약 0.25 log(0.8스톱) 어긋난다.**
검토자 두 명이 K 상수를 달리 잡아 0.54스톱과 0.83스톱으로 다르게 계산할 만큼
정확한 값 자체가 불확실해서, 추정값을 박는 대신 손잡이만 만들어 뒀다.

**할 일.** 기준이 되는 "정답 스캔"이 생기면 이 상수 **하나만** 한 번 맞춘다.
A1의 표본이 그 역할을 할 수 있다.

**주의.** 필름마다 다른 값을 주면 안 된다. 전역 상수다 — 필름별로 넣으면 감도
차이를 이중 적용하게 된다.

### A3. 필름 추가 (B/C등급)

`tools/tds-catalog.md`에 64종을 등급별로 정리해 뒀다. A등급은 추출 완료.
다음 후보는 색감이 특이하다고 평가되는 것 우선.

- Kodak Vericolor III
- Kodak Vision3 500T (텅스텐 — 현재 데이라이트 필름만 있다)
- Kodak Royal Gold 200 / Gold 200 (1998년판)
- Fuji Reala / Pro 400H
- Agfa Optima II

**방법.** `tools/extract_tds_curves.py`. 사용법과 함정 7가지는 `tools/README.md`.
출력에서 반드시 확인할 것 — 채널 간 크기 관계(B>G>R, 리버설은 예외), 눈금 교차
검증 결과, γ 값이 상식적인 범위인지.

**함정.** 지금까지 실제로 겪은 것들이라 반복될 가능성이 높다.

- 푸시 현상 곡선을 본 곡선으로 착각 (Portra 800에서 γ 136이 나왔다)
- **인화지 필름을 카메라 네거티브로 오인** (UltraMax 800 = E-7024는 컬러 인화
  필름이다. 125px 카탈로그가 잘못 분류해 뒀다)
- x축 라벨 순서 오타, 인접 열 라벨 누출

**검증.** 추출한 곡선에서 ISO를 역산해 표기값과 맞는지 본다(Portra 400 → 399.5,
Gold 200 → 207.2가 나왔다).

---

## 우선순위 B — 아키텍처 이분화 마무리

v1과 v2의 역할을 나누기로 했다(v2plan §1.5). v2 쪽(프로파일 생성)은 끝났고
**v1 쪽이 남았다.**

### ~~B1. 레거시 그레이딩을 JPEG 후처리용으로 최적화~~ — 해결 (v1.5)

역할 분담은 이렇다.

- **v2(필름 엔진)** — raw/16bit에서 프로파일·LUT을 만든다. 색을 정한다
- **v1(마감)** — 그렇게 나온 **JPEG에 할레이션·그레인을 얹고 약간의 보정**을 한다

조사해 보니 **코드는 이미 8bit에서 정상이었다.** `grading.js`는 조정 레이어 +
0~255 커브 좌표라 심도 독립이고, `halation.js`·`grain.js`도 마찬가지다.

진짜 문제는 **그 모드가 UI에서 읽히지 않는 것**이었다. 필름을 끈 상태가 "기능
없음"으로 보여서, 8bit JPEG을 열어도 필름을 켜 둔 채 쓰다가 밴딩을 보게 된다.

고친 것:

- 필름을 끄면 카드가 **"마감 모드"** 임을 설명한다
- `apply.validate`가 **마감 모드에서는 비트 심도를 경고하지 않는다.** 8bit는 그
  경로의 정상 입력이다. 정상 입력에 경고를 띄우면 진짜 경고를 무시하게 만든다
- 필름 엔진이 켜져 있을 때의 경고 문구에 "색이 이미 정해진 파일이라면 필름을 끄고
  마감만 쓰라"는 안내를 넣었다

**미결.** 컬러휠 UI를 어느 쪽에 둘지. 지금은 필름 엔진 위에 얹혀 있다.

### B2. `.cube` 폴백 색공간 정리

`core/io/cube.js`의 `acr`(ProPhoto 원색 + sRGB 톤 응답) 선택지는 **더 이상 쓸
곳이 없다.** New Profile 대화상자에 색공간 드롭다운이 있고, 프로파일은 이제 직접
생성하기 때문이다.

드롭다운이 없는 경로(구버전 ACR, SDK 직접 생성)를 만날 가능성 때문에 폴백으로
남겨 뒀다. **그런 경로가 없다고 확인되면 지운다.** 지금 지우면 안 된다.

---

## 우선순위 C — 배포

### ~~C1. 릴리스 빌드에서 `.git` 제외~~ — 해결 (v1.4.0-beta)

v1.2.0 배포판에 `.git`이 통째로 들어가 있었다. 원인은 `uxp plugin package`가
**manifest가 있는 폴더를 통째로 담고 제외 옵션이 없다**는 것. 저장소 루트에서
그냥 돌리면 이렇게 된다.

`tools/build-ccx.js`가 런타임 파일만 `build/`에 모아 거기서 패키징한다.
**13.2MB → 90KB** (`.git`이 12.6MB, 전체의 98%였다).

```bash
uxp service start          # 별도 터미널. 떠 있어야 packaging이 된다
node tools/build-ccx.js
```

### C2. 프로파일 배포 형태 결정

지금은 사용자가 "세트 전체" 버튼으로 직접 생성한다. 대안은 미리 만든 16개를
릴리스에 동봉하는 것.

- **직접 생성** — 엔진을 고쳐도 항상 최신. 대신 버튼을 눌러야 함
- **동봉** — 설치 직후 바로 쓸 수 있음. 대신 엔진 수정 시 재배포 필요

지금은 전자가 맞다고 본다. 엔진이 계속 바뀌는 단계이기 때문. 데이터가 안정되면
후자를 추가한다.

### C3. 정식 v1.4.0

A1이 해결되면 beta를 뗀다. A1 없이는 "필름 색을 재현한다"는 주장의 절반이
튜닝값에 기대고 있다.

---

## 우선순위 D — 나중에

### ~~D1. 포맷 인지 그레인~~ / ~~D2. 기준 해상도~~ — 해결 (v1.5, `core/optics/format.js`)

입자 크기는 **필름면에서 고정**이고, 최종 이미지에서 달라 보이는 것은 확대율
차이다. 그 물리를 그대로 넣었다.

```
px_per_mm = 이미지_긴변_px / 프레임_긴변_mm
grain_px  = (유효입자_µm / 1000) × px_per_mm
```

포맷 5종(35mm / 6×4.5 / 6×6 / 6×7 / 4×5). 슬라이더 0~100은 2~42µm이고 기본값
20이 10µm — Portra 400의 유효 입자 크기다. v2plan 4.5의 검증표를 정확히 재현한다.

기준 해상도는 원안의 2단계 대신 **3단계**로 넣었다(문서 / 6000 / 9000). "문서
그대로"가 물리적으로 옳은 기본값인데 2단계로는 표현할 수 없었다. 고정 기준은
작업 파일과 최종 출력 크기가 다를 때(웹 축소 등) 쓴다.

곁들여 해결된 것:

- **할레이션도 포맷을 안다.** 산란 거리도 필름면에서 고정이므로 같은 물리다.
  35mm를 1.0으로 놓은 배율이라 기존 프리셋 결과가 그대로 재현된다
- **서브픽셀 입자 처리.** `grain_px < 1`이면 블러를 끄고 노이즈 amount로 환산한다
- **DPI.** `doc.resolution`을 읽어 인쇄 크기를 안내한다. 없으면 300 가정.
  그레인 계산에는 들어가지 않는다

⚠️ **35mm 6000px에서 그레인이 기존 대비 약 1.9배 굵어진다.** 기존 계산(긴 변
3000px 정규화 × 계수 2.2)에 물리적 근거가 없었으므로 이 변화가 교정이다. 기존
룩을 유지하려면 크기 슬라이더를 낮춘다.

### D3. 영상용 Rec.709 `.cube`

`src/cubeexport.js`의 헤더 규약을 지켜뒀으므로 색공간 항목과 재샘플링만 추가하면
된다. 우선순위는 낮다 — 사진에 집중하기로 확정했다.

### D4. 라이선스 결정

개발 과정에서 정하기로 했다(v2questions 답변). 지금까지 지킨 제약:

- 제조사 TDS PDF는 재배포하지 않는다 (`.gitignore`에 `tools/tds/`, `*.pdf`)
- GPL/NC 데이터 소스를 피해 선택지를 열어 뒀다
- 상표는 명목적 사용만 (프리셋 이름). 로고·서체 없음, README와 정보 화면에 고지

이 제약을 유지하는 한 어느 라이선스든 고를 수 있다.

---

## 작업 재개 체크리스트

```bash
git log --oneline -3
node tools/sync-libs.js      # core/ 를 고쳤으면 반드시
node tools/check.js          # 문법·경계·로드·정합성. 3초
```

실기:

```
Photoshop 실행 → UDT에서 apps/engine/manifest.json, apps/finish/manifest.json 로드
```

**막히는 지점 세 개** — 전부 겪은 것들이다.

| 증상 | 원인 |
|---|---|
| `Cannot read property 'loadPlugin' of undefined` | `uxp service`가 포트 14001 점유. 끄고 UDT 재시작 |
| 엔진만 로드 실패 | 설치판 `com.filmsim.photoshop`과 ID 충돌. 설치판 제거 |
| 앱이 `core`를 못 찾음 | `sync-libs.js`를 안 돌렸다 |

**검증할 때 주의.** 대칭점(코너·원점·항등)만 보면 틀린 가설도 통과한다. 프로파일
포맷 규명에서 이것 때문에 한참 헤맸다. 전수 오차를 먼저 재고, 값이 실제로 변하는
축을 따라 훑는다 (`UXP-NOTES.md` 8절).
